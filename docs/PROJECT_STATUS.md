# SRTP 当前项目状态

**状态日期：2026-09-05**

## 结论

Windows / macOS 环境检测阶段已由 Pull Request #1 完成交付。本地 CLI 检测与选择阶段的五项退出条件现已满足；本轮成果经 [Pull Request #2](https://github.com/trydays/srt/pull/2) 交付至 `main`。这不是发布或打包二进制声明。

## 本地 CLI 阶段：内部完成

- 仅扫描 Codex CLI、Claude Code、Gemini CLI 三项固定 CLI，使用固定 `--version` 探测。
- 主进程、preload 与环境页通过专用的读取、重扫、选择方法打通；没有新增任意 CLI 调用面。
- 全局默认值只能由用户明确选择；已选 CLI 消失时清除，绝不自动选择或切换。
- 验证已完成：单元测试 **98/98**、Electron E2E **7/7**、当前 Mac 真实烟雾 **1/1**；实际 Mac 扫描到 Codex CLI 与 Claude Code，最终独立审查为 **Ready to merge: Yes**。
- 实现、测试、审查和修正均以远低于此前拒绝的 4–8 小时估计的工作量完成，未新增平台框架或无关重构。

## 用户可见完成

- 环境页只显示实际检测到的本地 CLI，并说明目前仅支持三种固定 CLI。
- 空结果精确显示“未扫描到可用本地 CLI”，不显示缺失工具灰卡、安装、登录或模型控制。
- 用户可显式选定全局默认值；重扫保留仍可用的选择，失效后清除。
- 无论扫描、空结果或未选择，用户都可继续进入主页和编辑器。

## 五项退出条件

1. **已满足**：三种固定 CLI 被扫描，且页面只显示成功检测到的结果。
2. **已满足**：空状态精确显示“未扫描到可用本地 CLI”。
3. **已满足**：默认值只能由用户明确选择、可保存，且从不自动选择或切换。
4. **已满足**：先前选中的 CLI 消失时会清除选择，要求新的明确选择。
5. **已满足**：CLI 能力始终可选，不影响 SRTP 其他功能的进入和使用；聚焦测试与 Mac 验证通过。

## 后续独立阶段

- 调用所选 CLI、自然语言到精确编辑指令转换和 Remotion 执行集成。
- CLI 安装、登录/认证、模型控制，及更多 CLI 或自定义注册表。
- 真实 Windows 烟雾与平台级验证。
- 偏好文件规范化和重启级持久化覆盖。
