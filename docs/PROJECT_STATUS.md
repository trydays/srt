# SRTP 当前项目状态

**状态日期：2026-09-08（第六周期开发、本地回归及两次真实 AI 整链验收完成）**

## 结论

前五周期已接通统一执行、区间调色、翻转/中心缩放、静态文字/矩形及组合透明度/缩放动画，均沿用 `ProjectEditing → EditDocument → RenderGraph`。第六周期已在同一链路接通颗粒与暗角，并在本轮获准后通过两次真实 Claude CLI、正式桌面、上下文、撤销及实际导出的整链验收；不为每种用户效果另建执行流程。固定翻译与真实调用证据分别记录。用户个人素材的观感验收仍待进行。

## 已完成的能力（周期 1–4）

- 全片字幕：本地识别、全文编辑、保存草稿、应用、刷新恢复、重新生成及撤销。未应用草稿不进入成片。
- 区间调色：色温、亮度、饱和度、对比度；多个步骤按顺序叠加。
- 区间画面变换：水平/垂直翻转、0.25–4 倍中心等比例缩放，放大裁边、缩小补黑边。
- 静态图层：纯色矩形和纯文本可组合成卡片，按顺序遮挡；位置相对于视频内在画幅，支持指定出现区间，字幕在图层上方。
- 一次自然语言请求可包含多个步骤，整体提交并撤销最近一次请求；不合法的后续步骤不会留下半次编辑。刷新恢复，后续 AI 请求带上当前项目与已有操作，明确只输出本次新动作。
- 原有配色和总体布局保留。AI 能力目录仅公开已经接通执行、预览与导出的能力，不承诺任意算法或效果都已实现。

## 实际视频导出

- 右上角“导出视频”→ 另存为 → 实际 FFmpeg 成片，有进度与取消；保护源文件和已有目标，未应用字幕草稿会阻止导出。
- 固定 MP4 / H.264 / AAC / yuv420p，保留原画幅、时长和音轨。源画面调色/变换/颗粒/暗角在下，文字/矩形按编辑顺序覆盖，字幕最后叠加。
- AI 只输出受校验的结构化数据；软件生成滤镜和固定文字文件，不执行模型提供的 Shell、路径或滤镜字符串。

## 第五周期：组合透明度与缩放动画

- 用户可见：文字与矩形组成一个平面组合，整体淡入/淡出、等比例放大和固定轻微回弹；按媒体时间播放和拖动，项目重开后恢复，整次请求可以撤销，实际 MP4 保留相同动画。每次请求仍只显示一条历史/时间轴记录，内部正确统计组合中的元素数。
- 内部完成度：共享关键帧、组合图层、严格参数校验、六个能力适配器、保存/撤销、预览、实际导出全部接通。默认 AI 目录已开放 `visual.group@1`，完整参数模式和已应用动画参数进入后续提示词；没有效果模板表或新的执行/存储系统。
- 透明度 0–1、组合缩放 0–2 倍，可给常量或 2–16 个关键帧；关键帧从组合起点计时，仅线性、缓出和固定轻微回弹。子元素先在视频内在画幅中合成，再整体变化；超出画幅被裁掉的文字不会因缩小重新出现。
- 范围仅新建组合的透明度、缩放及固定中心点；无位移/旋转关键帧、嵌套组合、引用旧图层改造、图片、个人技能库或新属性面板。源视频的已有缩放仍是静态区间操作。
- 约 07:56 UTC 开始，08:58 UTC 首个桌面动画场景通过，09:15 UTC 独立本地整链通过，均在三小时可见检查/六小时止损线之前。随后获准补做真实 AI 验收并通过，没有增加功能或自动进入第六周期。

## 已确认的验证与备份

- 第四周期最终主代理复跑：实际 FFmpeg Node/媒体 **282/282**、Electron **54/54**，均零失败、零跳过；随后两次真实 Claude → 正式桌面 IPC → 项目 → FFmpeg 验收通过，使用人工素材，不上传视频。
- 第四周期实际成片 640×360、4 秒、有音轨；预览/成片 21 个实心区域最大通道差 2/255，未提高 7/255 门限。第二条仅新增编辑，上下文/撤销/重开通过；不代表所有未来模型请求均保证成功。
- 第五周期最终主代理独立复跑：真实 FFmpeg Node/媒体 **314/314**，Electron **57/57**，零失败、零跳过；全周期独立只读审查 `b530e2d..443f523` 无待修项。
- 正式桌面/IPC/实际 FFmpeg 的隔离整链通过：固定两条测试配方、一个动画组合，再新增纯文字并撤销，项目和上下文恢复一致。实际成片 640×360、4 秒、H.264/AAC；六个动画时刻共 1013 个实心区域采样最大通道差 **7/255**，几何误差在既定 1 像素门限内，起止帧保持半开区间。
- 最初的独立整链在非整数帧边界取到了前一源帧；不加载产品代码的页面证实是浏览器将跳转时间向下舍入到微秒。仅纠正临时验收脚本的帧对齐后整链通过，产品代码和容差均未改，失败证据保留。
- 本地成片与证据：`/tmp/srt-cycle5-local-final.hri4Fa/`；完整最终桌面产物：`/tmp/srt-cycle5-final.6nyXph/e2e/`。临时验收媒体不入库，固定测试翻译不计作真实 AI。
- 获准后的真实 Claude 验收：恰好两次调用（50.186 秒、10.288 秒）、一次实际导出，脚本 PASS/退出码 0，无页面错误。第一条自主生成文字/矩形整体淡入、半尺寸放大回弹、保持、淡出的配方；第二条收到完整已有组合/关键帧上下文，只新增红色文字，撤销并重开后原组合不变。
- 真实 AI 成片为 640×360、4.000 秒且带音轨的 H.264/AAC MP4。13 个预览时刻、10 个成片帧；六个有效动画帧共 765 个实心区域采样最大通道差 **6/255**，未放宽 7/255 或 1 像素门限。实际成片及证据：`/tmp/srt-cycle5-real.qMHHAs/`。本次不改产品代码、不重复运行此前已通过的 314/314 与 57/57 全套回归。
- 第六周期开工前已按用户要求将完整第五周期版本 `bdfc4225b4935284cb0ecdac6b281b0b97ff98bc` 推送并核对到 GitHub `trydays/srt` 的 `feature/subtitle-export-cycle0`。第六周期暂只在本地开发，不自动推送或合并主分支。

## 第六周期：区间颗粒与暗角

- 实现目标：AI 可将画面颗粒、四角压暗与已有调色、变换、图层组合，指定时间区间和强度，使用现有统一执行链完成预览、保存、整次撤销和实际视频导出；不增加“复古”等效果名到预设脚本的映射。
- 内部完成度：新增 `video.noise@1`、`video.vignette@1` 两个完整适配器，默认 AI 目录扩展到八项；共享参数/像素语义、现有两缓冲源画面预览、受控 FFmpeg 导出均已接上。独立复审发现的中断跳转后换素材状态残留已用失败回归复现并修复；完整周期审查 `bdfc422..53dee81` 无待修项。
- 用户可见成果：人工视频可正常播放和前后拖动，颗粒与暗角只在指定区间出现，组合顺序保留；保存重开、后续新增文字、撤销最新请求后再重开均正确。首个可见检查约 10:29 UTC，正式入口整链及阅片约 11:08 UTC 完成，早于两小时检查/四小时止损线。
- 能力边界：单色细颗粒强度 0–1，固定随媒体时间更新；居中圆形暗角强度 0–1。零强度不改变画面，区间为包含起点、不包含终点。源画面效果按顺序叠加，卡片/文字和字幕在其上方；原配色与布局不变。
- 不含颗粒尺寸/种子/频率调节、移动暗角、模糊、LUT、Shader、任意代码/算法安装、新面板或个人技能库。不会自动进入第七周期。
- 主代理最终独立复跑：真实 FFmpeg Node/媒体 **335/335**、完整 Electron **63/63**，均退出码 0、零失败、零跳过。另用实际页面三组 PNG 对照验证源效果显示且不透明卡片位于其上方，无截图色彩容差放宽。
- 正式应用本地整链 **PASS**：两条固定测试翻译经生产解析/IPC/项目/预览，一次实际导出在第二条文字已撤销后进行。成片 **640×360、4.000 秒、H.264/AAC**，导出约 **6.18 秒**。三个有效时刻预览/成片颗粒相关性 **0.9646–0.9693**、强度保留比 **0.9295–0.9313**；三个区间外时刻采样误差 **0/255**。这些是人工素材结果，不是实时导出或长片性能承诺。
- 成片与正式入口证据：`/tmp/srt-cycle6-local.bAetFB/`；最终完整桌面产物：`/tmp/srt-cycle6-final.zHLxVC/e2e-final/`。临时媒体与隔离项目不入库，已查看实际预览和成片抽帧。
- 获准后的第六周期真实 Claude CLI 两次调用通过（12.497 秒、20.073 秒）：AI 自主组合区间调色、颗粒 0.45、暗角 0.55；第二条收到准确项目状态，只新增红色文字，撤销重开正确。一次真实导出约 6.266 秒，640×360、4 秒、H.264/AAC；有效区域颗粒相关性 0.948515–0.950298、强度保留比 0.920621–0.922846，区间外采样误差为零。独立只读证据核查通过，主代理已阅预览及成片帧。
- 真实调用证据与视频：`/tmp/srt-cycle6-acceptance.MwZVAY/`。仅人工文字和人工项目摘要进入翻译；视频不上传。重开/后续指令/撤销及默认生产导出链已验收；用户个人素材、原生保存对话框人工点验和打包发行版仍单独待做。最新上传前另复跑 Node/实际媒体 335/335，通过且零跳过；产品代码未改，之前完整 Electron 63/63 不冒称本次复跑。

## 限制

- 仍只保留最近一次请求的撤销；不包含任意代码、新算法自动安装、通用 DAG、粒子系统或个人技能积累复用。
- 固定 Heiti SC 纯文本（显式换行），不支持富文本、自动避让或智能排版；不承诺预览与成片字形抗锯齿逐像素一致、HDR 或专业色彩管理。
- 两次真实 Claude 动画/后续新增请求已获准并通过，但不能保证所有未来模型请求均成功；首条约 50 秒，响应速度仍有等待成本。用户个人素材的观感验收、手机旋转元数据片段复验仍单独保留。
- 程序化指定导出目标不等于人工点击原生保存对话框。已打开的旧应用需重新载入新代码。
- 当前真实整链验收运行开发态 Electron，不代表打包后的安装发行版已验收。

## 日志

完整研发记录见 [docs/DEVELOPMENT_LOG.md](DEVELOPMENT_LOG.md)。
