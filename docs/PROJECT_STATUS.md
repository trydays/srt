# SRTP 当前项目状态

**状态日期：2026-09-09（Remotion R1–R4 实现与本地自动化验收完成；R4 真实 AI 未执行，用户人工观感待确认；第七周期真实 AI 验收仍未全部通过）**

## 结论

前六周期已接通统一执行、区间调色、翻转/中心缩放、静态文字/矩形、组合动画、颗粒与暗角，均沿用 `ProjectEditing → EditDocument → RenderGraph`；第六周期两次真实 Claude 整链验收完成并已备份到 GitHub。第七周期新增个人剪辑技能的显式保存与跨项目调用，本地桌面、实际成片、完整回归及全周期审查已通过。随后获准的两次真实 AI 复用测试已执行：横版重新推导、应用、重开与导出通过；竖版在翻译阶段触发 60 秒超时，未进入编辑执行，因此真实 AI 整轮验收未通过。固定翻译不冒充真实 AI，用户个人素材观感仍待验收。

## Remotion R4：通用底板样式与本地验收完成

- 已收到用户本地视频 `37875025116-1-192.mp4`（13 分 29 秒，1280×720、30 fps），抽帧定位约 02:11–02:24 的信息卡和 07:00 起的技能卡，并核对现有组合、动画和图层样式能力。
- 用户已确认先补通用底板样式，现已接入圆角、内描边、底板填充透明度，独立 shape 与 group 子层共用同一 Remotion 绘制；沿用已有时间区间和组动画。参数自动进入 AI 目录/解析，旧存档与撤销快照保持无写回兼容。图标、阴影、文字样式、画中画没有混入本轮。
- **内部完成度：** Task 1/Task 2 本地实现与独立审查均通过；主代理最终全真实开关 Node/媒体 **418/418，12.883 秒**，Remotion 桌面 **16/16，1.8 分钟**；补充音频/技能验证后最终完整桌面 **83/83，5.3 分钟**，零失败、零跳过。其中 16 项 Remotion、67 项显式旧路径回归，不混称全部新引擎验收。原界面、依赖和项目格式未改。
- **用户可见成果：** 四秒示范 `/tmp/srt-remotion-r4.bX0Rwi/r4-card-style-preview.mp4` 为 1280×720、24fps、96 帧、带音轨的真实 Remotion 成片。正式桌面另完成横竖版四张错时卡片、透明填充保留边框/文字、旧存档不写回、追加不同样式请求后保存重开及撤销、技能保存后重开查看/选择，以及实际导出。两个画幅均保留四秒原合成声；已查看最终预览和成片抽帧。证据在 `e2e-final/remotion-card-style-flow-R-1309b-cards-in-both-aspect-ratios/`（同临时根目录）。
- **剩余边界：** 固定配方验证不等于真实 AI 联调；本轮 AI 问题尚未获明确答复，没有执行。修改文案/样式通过新增请求验证，不宣称已有卡片可原位编辑。不是固定卡片模板或参考片逐像素复刻；字体/屏幕颜色限制、用户观感、Windows/安装发行版仍待验收。[R4 小计划](superpowers/plans/2026-09-09-remotion-r4-card-style.md) 已记录证据；未上传参考视频、未提交或推送，R1/R2/R3 成果保留。

## Remotion R3：当前迁移范围

- **内部完成度：** R3 九项既定实现/自动化验收均完成。现有八种编辑能力——调色、画面变换、颗粒、暗角、独立矩形、独立文字、组合动画、已应用字幕——全部由同一个 `SrtComposition` 负责 Player 和实际 MP4。正常入口不再切回旧引擎或生成旧 render-recipe，不支持的图明确报错；旧代码仅保留作显式隔离的历史回归。
- **用户可见成果：** 可在同一片段叠加源画面效果、卡片和字幕；按时间区间生效，保存重开、字幕改字/应用、个人技能保存/查看/选择、取消后再导出、整次撤销和切换项目均通过。原界面配色、布局、项目格式及仅最近一次请求撤销的边界未改变。
- 最终主代理复跑：全真实开关 Node/媒体 **410/410**（12.753 秒），完整桌面 **82/82**（5.1 分钟，其中 15 项 Remotion、67 项显式旧路径回归），真实 Mac 环境页 **1/1**（1.7 秒），全部零失败、零跳过。最终独立审查无未解决 Critical/Important。
- 真实检测已接正式预览/导出：检查 Remotion 包与版本、Player/renderer 构建、本地渲染浏览器和媒体探测工具；不再仅凭 Node/npm 报可用，不自动下载。已修复切换画面导致共享文件句柄被旧请求关闭的桌面异常，以及浏览器查找依赖启动目录的问题。
- 可查看成片：`/tmp/srt-remotion-r3.TvI79k/e2e-final/remotion-source-effects-fl-47943--undoes-all-eight-abilities/r3-landscape-first-export.mp4` 和 `remotion-source-effects-fl-bd71e--undoes-all-eight-abilities/r3-portrait-first-export.mp4`。横版 640×360、竖版 360×640，均 24fps、96 帧、视频 4 秒；横版一条 AAC 音轨，竖版无音轨。各目录还有再次导出、实际预览和抽帧；主代理已独立探测四份文件并查看最终画面。
- **能力边界：** 这是现有能力的渲染迁移，不是新增任意代码或新特效生成。使用合成素材与明确标记的固定翻译/字幕，未调用真实 AI、未上传私人视频；此前真实 AI 竖版超时仍未解决。字体外观、屏幕色彩解释、个人素材、原生保存框人工体验、Windows/安装发行版仍待验收。R4 此后已完成上方所述底板样式本地扩展；R1/R2/R3 未提交成果均保留。

## Remotion R2：历史验收记录（R3 已扩展其节点范围）

- **内部完成度：** R2 场景绘制、入口接通及桌面验收三项完成。新增独立矩形、独立文字、已应用字幕，与 R1 动画组共用同一个 `SrtComposition` 预览及实际导出。产品只改四个必要文件，没有新增渲染器、存储格式、IPC 或字体系统。
- **用户可见成果：** 同一视频可同时显示文字、底板、组合动画和字幕；改字后“保存”仅保留草稿且继续阻止导出，“应用”才同步改变预览与成片。重开恢复草稿/应用状态，最近一次请求撤销仍整次生效；点击字幕不会再叠出旧字幕层。界面颜色和布局未改。
- 主代理最终复跑：全真实开关 Node/媒体 **395/395**（6.143 秒），Remotion 桌面 **12/12**（1.0 分钟），原有完整真实媒体桌面 **67/67**（3.6 分钟），全部零失败、零跳过。独立场景审查和整轮审查均无待修 Critical/Important；一个字幕节点区间隔离断言建议为非阻断覆盖限制。
- 最终横、竖屏各有字幕修改前后两份真实 MP4：640×360 / 360×640，24fps、96 帧、视频 4.000 秒，H.264（探测为 yuvj420p）/AAC 48kHz；音轨及容器 4.010667 秒，差小于一帧。主代理独立探测四份文件，并查看最终两种画幅预览及前后成片帧，文字可读且字幕修改生效。
- 证据：`/tmp/srt-remotion-r2.OeB8le/remotion-final/`（含 `r2-landscape-original.mp4`、`r2-landscape-edited.mp4`、对应 portrait 文件和截图）；旧桌面回归在 `legacy-final/`。均用合成短视频、固定且明确标记的翻译/识别结果及测试指定保存路径；不是新一轮真实 AI、本地识别模型或原生保存框人工验收。
- R2 当时按整项目选择引擎：仅 source/group/shape/text/subtitle 的项目走 Remotion，含启用源效果时走旧路径。该迁移限制已由上方 **R3** 解除；参考卡片扩展随后进入上方 **R4**。
- R1 的字体实际命中及屏幕颜色解释路径差异仍保留，不能声称逐像素/字形一致。用户素材观感、Windows 和安装发行版仍未验收。本轮没有真实 AI 调用、没有上传用户视频、没有提交或推送；既有 R1 未提交改动原样保留。

## Remotion R1：历史验收记录（R2 已扩展其节点范围）

- 保留现有 EditDocument / RenderGraph、保存/撤销、聊天、个人技能和 UI 配色。仅 `source.video@1` + `visual.group@1` 项目成对使用同一个 Remotion Composition 预览和导出；其余启用节点仍让整项目走旧路径，不是八能力已经全部迁移。
- 原播放/暂停、拖动、速度、音量接同一个播放控制器；旧视频不同时发声。临时媒体 URL 不写入 v1 存档。
- 已从真实桌面按钮输出横版 640×360、竖版 360×640 的卡片 MP4；原声音保留，撤销后再次导出的卡片消失。实际取消恢复按钮且不留下目标文件；源文件与已有目标保护保留。
- 本轮最终验证：全开关 Node/真实媒体 **389/389**；原有 67 个桌面用例先 56 通过/11 跳过，再以真实媒体补测 12/12 覆盖全部跳过项。新增 Remotion 桌面 **9/9**（41.4 秒，零失败/跳过），含横竖屏实际导出、撤销后导出、取消、重开、重传、草稿保护和错误状态。
- 预览与成片五时点内容检查维持 12/255 容差；反向跳转截图严格一致。截图与视频存在颜色解释路径差异，数值检查使用统一 sRGB 内容合成口径，不宣称最终屏幕颜色或字形抗锯齿逐像素相同。主代理已查看中文卡片截图及实际成片帧，用户观感仍待确认。
- 已观察到中文“重点”两端字形不同：共享 `Heiti SC` 声明及字体加载等待，但尚未绑定或确认同一实际字体文件。内容完整可辨，位置和动画已验证；字体外观一致性是明确限制，不以测试通过掩盖。
- 最终成片与截图位于 `/tmp/srt-remotion-r1.xDXjmb/accepted-remotion-e2e/`：横版 640×360、竖版 360×640，视频均为 24fps、96 帧、4.000 秒，AAC 音频 4.010667 秒（相差小于一帧）。素材为人工合成验收短片，不是参考视频效果复刻。
- 本轮没有调用真实 AI、没有上传用户视频、没有提交或推送 GitHub；未开始 R2/R3。临时合成视频和截图在 `/tmp/srt-remotion-r1.xDXjmb/`，不入库。人工素材观感、原生另存为人工点击、Windows/安装包验收另行保留。

## 已完成的能力（周期 1–4）

- 全片字幕：本地识别、全文编辑、保存草稿、应用、刷新恢复、重新生成及撤销。未应用草稿不进入成片。
- 区间调色：色温、亮度、饱和度、对比度；多个步骤按顺序叠加。
- 区间画面变换：水平/垂直翻转、0.25–4 倍中心等比例缩放，放大裁边、缩小补黑边。
- 静态图层：纯色矩形和纯文本可组合成卡片，按顺序遮挡；位置相对于视频内在画幅，支持指定出现区间，字幕在图层上方。
- 一次自然语言请求可包含多个步骤，整体提交并撤销最近一次请求；不合法的后续步骤不会留下半次编辑。刷新恢复，后续 AI 请求带上当前项目与已有操作，明确只输出本次新动作。
- 原有配色和总体布局保留。AI 能力目录仅公开已经接通执行、预览与导出的能力，不承诺任意算法或效果都已实现。

## 实际视频导出

- 右上角“导出视频”→ 另存为 → 实际成片，有进度与取消；R1/R2 已覆盖的整项目走 Remotion，其余暂走已有 FFmpeg 路径。保护源文件和已有目标，未应用字幕草稿会阻止导出。
- 固定 MP4 / H.264 / AAC，保留原画幅、时长和音轨；Remotion R2 实际文件探测为 yuvj420p，旧路径为 yuv420p，不以同一名称掩盖全范围标记差异。源画面调色/变换/颗粒/暗角在下，文字/矩形按编辑顺序覆盖，字幕最后叠加。
- AI 只输出受校验的结构化数据；软件生成滤镜和固定文字文件，不执行模型提供的 Shell、路径或滤镜字符串。

## 第五周期：组合透明度与缩放动画

- 用户可见：文字与矩形组成一个平面组合，整体淡入/淡出、等比例放大和固定轻微回弹；按媒体时间播放和拖动，项目重开后恢复，整次请求可以撤销，实际 MP4 保留相同动画。每次请求仍只显示一条历史/时间轴记录，内部正确统计组合中的元素数。
- 内部完成度：共享关键帧、组合图层、严格参数校验、六个能力适配器、保存/撤销、预览、实际导出全部接通。默认 AI 目录已开放 `visual.group@1`，完整参数模式和已应用动画参数进入后续提示词；没有效果模板表或新的执行/存储系统。
- 透明度 0–1、组合缩放 0–2 倍，可给常量或 2–16 个关键帧；关键帧从组合起点计时，仅线性、缓出和固定轻微回弹。子元素先在视频内在画幅中合成，再整体变化；超出画幅被裁掉的文字不会因缩小重新出现。
- 范围仅新建组合的透明度、缩放及固定中心点；无位移/旋转关键帧、嵌套组合、引用旧图层改造、图片、个人技能库或新属性面板。源视频的已有缩放仍是静态区间操作。
- 约 07:56 UTC 开始，08:58 UTC 首个桌面动画场景通过，09:15 UTC 独立本地整链通过，均在三小时可见检查/六小时止损线之前。随后获准补做真实 AI 验收并通过，没有增加功能或自动进入第六周期。

## 已确认的验证与备份

- 第四周期最终主代理复跑：实际 FFmpeg Node/媒体 **282/282**、Electron **54/54**，均零失败、零跳过；随后两次真实 Claude → 正式桌面 IPC → 项目 → FFmpeg 验收通过，使用人工素材，不上传视频。
- 第四周期实际成片 640×360、4 秒、有音轨；预览/成片 21 个实心区域最大通道差 2/255，未提高 7/255 门限。第二条仅新增编辑，上下文/撤销/重开通过；不代表所有未来模型请求均保证成功。
- 第五周期最终主代理独立复跑：真实 FFmpeg Node/媒体 **314/314**，Electron **57/57**，零失败、零跳过；全周期独立只读审查 `b530e2d..443f523` 无待修项。
- 正式桌面/IPC/实际 FFmpeg 的隔离整链通过：固定两条测试配方、一个动画组合，再新增纯文字并撤销，项目和上下文恢复一致。实际成片 640×360、4 秒、H.264/AAC；六个动画时刻共 1013 个实心区域采样最大通道差 **7/255**，几何误差在既定 1 像素门限内，起止帧保持半开区间。
- 最初的独立整链在非整数帧边界取到了前一源帧；不加载产品代码的页面证实是浏览器将跳转时间向下舍入到微秒。仅纠正临时验收脚本的帧对齐后整链通过，产品代码和容差均未改，失败证据保留。
- 本地成片与证据：`/tmp/srt-cycle5-local-final.hri4Fa/`；完整最终桌面产物：`/tmp/srt-cycle5-final.6nyXph/e2e/`。临时验收媒体不入库，固定测试翻译不计作真实 AI。
- 获准后的真实 Claude 验收：恰好两次调用（50.186 秒、10.288 秒）、一次实际导出，脚本 PASS/退出码 0，无页面错误。第一条自主生成文字/矩形整体淡入、半尺寸放大回弹、保持、淡出的配方；第二条收到完整已有组合/关键帧上下文，只新增红色文字，撤销并重开后原组合不变。
- 真实 AI 成片为 640×360、4.000 秒且带音轨的 H.264/AAC MP4。13 个预览时刻、10 个成片帧；六个有效动画帧共 765 个实心区域采样最大通道差 **6/255**，未放宽 7/255 或 1 像素门限。实际成片及证据：`/tmp/srt-cycle5-real.qMHHAs/`。本次不改产品代码、不重复运行此前已通过的 314/314 与 57/57 全套回归。
- 第六周期开工前已按用户要求将完整第五周期版本 `bdfc4225b4935284cb0ecdac6b281b0b97ff98bc` 推送并核对到 GitHub `trydays/srt` 的 `feature/subtitle-export-cycle0`；本轮又完成第六周期真实验收后的 `a9a2bd7` 备份。当前第七周期仍仅本地提交，不自动再次推送或合并主分支。

## 第六周期：区间颗粒与暗角

- 实现目标：AI 可将画面颗粒、四角压暗与已有调色、变换、图层组合，指定时间区间和强度，使用现有统一执行链完成预览、保存、整次撤销和实际视频导出；不增加“复古”等效果名到预设脚本的映射。
- 内部完成度：新增 `video.noise@1`、`video.vignette@1` 两个完整适配器，默认 AI 目录扩展到八项；共享参数/像素语义、现有两缓冲源画面预览、受控 FFmpeg 导出均已接上。独立复审发现的中断跳转后换素材状态残留已用失败回归复现并修复；完整周期审查 `bdfc422..53dee81` 无待修项。
- 用户可见成果：人工视频可正常播放和前后拖动，颗粒与暗角只在指定区间出现，组合顺序保留；保存重开、后续新增文字、撤销最新请求后再重开均正确。首个可见检查约 10:29 UTC，正式入口整链及阅片约 11:08 UTC 完成，早于两小时检查/四小时止损线。
- 能力边界：单色细颗粒强度 0–1，固定随媒体时间更新；居中圆形暗角强度 0–1。零强度不改变画面，区间为包含起点、不包含终点。源画面效果按顺序叠加，卡片/文字和字幕在其上方；原配色与布局不变。
- 不含颗粒尺寸/种子/频率调节、移动暗角、模糊、LUT、Shader、任意代码/算法安装、新面板或个人技能库。不会自动进入第七周期。
- 主代理最终独立复跑：真实 FFmpeg Node/媒体 **335/335**、完整 Electron **63/63**，均退出码 0、零失败、零跳过。另用实际页面三组 PNG 对照验证源效果显示且不透明卡片位于其上方，无截图色彩容差放宽。
- 正式应用本地整链 **PASS**：两条固定测试翻译经生产解析/IPC/项目/预览，一次实际导出在第二条文字已撤销后进行。成片 **640×360、4.000 秒、H.264/AAC**，导出约 **6.18 秒**。三个有效时刻预览/成片颗粒相关性 **0.9646–0.9693**、强度保留比 **0.9295–0.9313**；三个区间外时刻采样误差 **0/255**。这些是人工素材结果，不是实时导出或长片性能承诺。
- 成片与正式入口证据：`/tmp/srt-cycle6-local.bAetFB/`；最终完整桌面产物：`/tmp/srt-cycle6-final.zHLxVC/e2e-final/`。临时媒体与隔离项目不入库，已查看实际预览和成片抽帧。
- 获准后的第六周期真实 Claude CLI 两次调用通过（12.497 秒、20.073 秒）：AI 自主组合区间调色、颗粒 0.45、暗角 0.55；第二条收到准确项目状态，只新增红色文字，撤销重开正确。一次真实导出约 6.266 秒，640×360、4 秒、H.264/AAC；有效区域颗粒相关性 0.948515–0.950298、强度保留比 0.920621–0.922846，区间外采样误差为零。独立只读证据核查通过，主代理已阅预览及成片帧。
- 真实调用证据与视频：`/tmp/srt-cycle6-acceptance.MwZVAY/`。仅人工文字和人工项目摘要进入翻译；视频不上传。重开/后续指令/撤销及默认生产导出链已验收；用户个人素材、原生保存对话框人工点验和打包发行版仍单独待做。最新上传前另复跑 Node/实际媒体 335/335，通过且零跳过；产品代码未改，之前完整 Electron 63/63 不冒称本次复跑。

## 第七周期：个人剪辑技能

- 实现目标：用户明确保存一次满意编辑的意图、偏好和参考配方；跨项目查看、选择调用与删除。选择技能不会直接修改视频，发送请求后仍由 AI 结合当前视频重新推导，再走既有执行、预览、保存、撤销及导出链路。
- 内部完成度：技能存储/事务参考/提示词与翻译入口 `cd3662c`，界面和正常执行链衔接 `2fbf9cd` 已实现。两个任务的独立范围审查及全周期 `a9a2bd7..2fbf9cd` 最终审查均通过，无待修项。主代理在最终代码上独立复跑实际 FFmpeg Node/媒体 **349/349**、完整 Electron **67/67**，退出码 0、零失败、零跳过。
- 用户可见成果：成功记录展开后可“保存为技能”，填写名称、可编辑剪辑意图和偏好；输入区“我的技能”可查看、选择使用及删除。仅选择不会执行，光标返回输入框；可补充本次要求再发送。反问期间保留同一个技能上下文，提供退出操作，任务完成/失败后清除选择，不影响下一条普通请求。
- 保存仅取该成功请求在当前项目中仍已应用的步骤；不复制视频路径、项目身份或生成的字幕全文。技能跨本机项目保留，删除技能不移除已经应用的编辑。旧能力版本不可用时仍可查看/删除，由当前 AI 重新推导可用组合或反问，不兼容重放旧配方。
- 主代理独立正式入口整链 **PASS**：人工 8 秒参考的旧区间为 6–8 秒，通过真实保存/使用 UI 和两条明确的固定测试翻译，分别在横版 **640×360、4 秒** 的 3–4 秒、竖版 **360×640、3 秒** 的 2–3 秒应用新组合。两份实际 **H.264/AAC MP4** 保留各自画幅、时长和音轨；实际预览/成片抽帧已查看。重开、最新请求撤销、删除技能但保留旧编辑均通过，无页面错误。该结果证明本地衔接，不证明模型已自主完成跨项目适配。
- 上述本地固定翻译证据与成片：`/tmp/srt-cycle7-local.EZ8Azo/`；完整回归桌面结果：`/tmp/srt-cycle7-final.fcvIBq/e2e/`。这些是此前完成的验证；本次仅做真实 AI 联调及记录，未修改产品代码，也未重新运行全套回归。
- 获准后的真实 AI 联调：**恰好 2 次 Claude 翻译、1 次实际导出，整轮未通过**。两次请求携带相同技能意图、偏好和旧 6–8 秒参考，以及各自当前视频事实；仅发送人工文字/摘要，不上传视频。横版翻译 **38.940 秒**，生成新 3–4 秒的黑底白字“横版重点”组合，包含淡入淡出与轻微缩放；单编辑/单事务、重开恢复及实际导出通过。独立检查成片为 **640×360、4.000 秒、H.264/AAC**，抽帧可见卡片。
- 竖版翻译 **60.092 秒** 返回 `LOCAL_CLI_TRANSLATION_TIMEOUT`，未返回可执行指令；界面为“转换编辑指令失败／执行编辑未执行”。没有第二份成片，后续竖版适配、撤销及删除技能隔离步骤未执行，不能用此前固定翻译的通过结果代替。脚本等待成功状态的 120 秒断言超时与产品 CLI 的 60 秒超时分别记录。
- 真实证据：`/tmp/srt-cycle7-acceptance.g6gjZj/`，含调用记录、完成/失败记录、横版成片及失败截图；主代理和独立审查均确认部分通过、整轮未通过。现有证据只能定位到 CLI 等待结果阶段，不能区分模型生成慢、网络等待或 CLI 卡住。两次调用授权已用完，未重试、未延长超时、未改变执行架构；进一步真实调用需新授权。真实 AI 验收门仍开放。
- 开发基线：`a9a2bd7f365a95378de54f96c4b254b0e29c0e1d` 已推送并独立核对到 GitHub `trydays/srt` 的 `feature/subtitle-export-cycle0`。第七周期改动暂留本地。实施计划见 `docs/superpowers/plans/2026-09-08-generic-ai-edit-cycle-7.md`。

## 限制

- 仍只保留最近一次请求的撤销；不包含任意代码、新算法自动安装、通用 DAG 或粒子系统。个人技能复用已接通，但不训练模型、不自动积累技能、不增加底层能力；无技能编辑、导入导出、云同步、共享或市场。
- 固定 Heiti SC 纯文本（显式换行），不支持富文本、自动避让或智能排版；不承诺预览与成片字形抗锯齿逐像素一致、HDR 或专业色彩管理。
- 两次真实 Claude 动画/后续新增请求已获准并通过，但不能保证所有未来模型请求均成功；首条约 50 秒，响应速度仍有等待成本。用户个人素材的观感验收、手机旋转元数据片段复验仍单独保留。
- 程序化指定导出目标不等于人工点击原生保存对话框。已打开的旧应用需重新载入新代码。
- 当前真实整链验收运行开发态 Electron，不代表打包后的安装发行版已验收。

## 日志

完整研发记录见 [docs/DEVELOPMENT_LOG.md](DEVELOPMENT_LOG.md)。
