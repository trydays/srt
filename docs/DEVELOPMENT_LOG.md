# SRTP 研发记录

## 2026-09-09：首页项目删除

按确认的收缩范围，仅新增首页项目卡片删除按钮及确认提示。删除指定项目列表项、对话、字幕、编辑状态及复合键字幕草稿；调整活动项目指针，不调用文件删除接口，不修改个人技能。读取异常停止，写入失败尝试回滚。没有加入重命名、备份、云同步或存储重构；顶部标签原有关闭行为不变。

验证：3 项 Node 测试通过（选择性清理、损坏数据不写、写失败回滚）；1 项隔离 Electron 测试通过（取消、确认、重新加载后不恢复、保留其他项目）。未操作用户真实项目，未推送或重启。用户可见成果：重新打开首页后可删除项目；内部完成：本轮限定实现和上述验收通过。

## 2026-09-09：官方口播要点技能模板

编辑页「我的技能」内置「口播要点自动卡片」，标记官方模板并保留原版；选择后通过现有对话提交。模板携带提炼意图与粉色毛玻璃卡片／大标题风格，不携带固定视频时间和配方。个人技能存储不被覆盖，成功修改可沿用现有另存入口。为无参考配方的技能上下文支持 null，个人记录仍要求有效配方。

验证：45 项相关 Node 测试及 5 项 Electron 技能流程测试通过，包含官方展示与选择、不写个人存储、个人保存删除、跨项目重推导和导出。Electron 使用固定 CLI 响应，不是本轮真实 AI／新视频验收。本轮未重启用户应用、未推送。

## 2026-09-09：像素风应用图标

按用户授权使用内置图片生成工具制作单个像素风图标：深蓝、米白、薄荷绿，视频画框／播放符号与剪辑时间线。生成后仅作尺寸和格式转换，保存 PNG、Windows ICO、macOS ICNS 至 app/assets。窗口使用 PNG，macOS 开发运行设置 Dock 图标，打包配置指定对应系统图标。未修改编辑器配色、功能或另建品牌系统。

核验图片格式与尺寸、主进程语法及打包配置路径；本轮未重新制作安装包或重启用户应用。素材随代码推送，不包含用户视频。

## 2026-09-09：细化时间接入完成，新视频真实全链路通过

按用户批准的收缩计划执行：现有 faster-whisper-small 分析模式启用词级时间，Python 按标点和 0.5 秒停顿分组；无可靠句界时保留词单元，词时间缺失／不完整时退回原段，不按时长均分。默认字幕输出维持原格式；新增 includeTiming 分析模式返回独立 timingSegments，经服务校验、IPC、前端和请求缓存进入现有 transcript。提示词说明细时间来自识别估计，可以按原文合并要点。未迁移历史项目或改变 Render Recipe。

测试先行：新分析模式在旧服务报 SUBTITLE_INVALID_OUTPUT，修复后相关 Node 测试 **98/98**、Python 分句测试 **3/3**、固定边界桌面回归 **9/9（42.6 秒）**。首次本地新视频分析获得四句时间清单，存于 `/tmp/srt-timing-20260909.md`；没有人工输入时间戳。

随后一次真实验收 **1/1 通过，约 1.7 分钟，零测试重试**。生产代码自动获取现有系统代理；第一轮 Codex 14905ms 返回 prepare，本地识别一次；第二轮 56800ms 返回三组合法视觉指令：第一组 1.76–5.22 秒、第二组 5.38–8.62 秒、第三组 9.40–16.02 秒。第三组将两句口播合并为一个语义要点，未再要求用户提供时间戳。识别两次的少量时间差说明它是估计，不声称毫秒级精度。

事务应用、正式预览、重新加载无额外模型调用、Remotion 导出和撤销均通过。导出为 17.6 秒、1280×720、H.264 + AAC 的 MP4；完整解码退出 0。已查看正式预览截图及导出第 3／7／13 秒画面，三组要点文字实际出现在左右位置，没有将原视频内置内容冒充新增效果。原音存在；精确感知同步与美术满意度留给用户观看，不追加 0.5 秒门槛。撤销后编辑数为 0。

内部完成：本轮采集、传递和真实执行验证通过。用户可见成果：真实导出文件 `/tmp/srt-timing-acceptance.dVqVes/e2e/real-keypoints-flow-real-s-9025d-nders-and-undoes-atomically/real-keypoints.mp4` 与时间清单。证据包含 before/applied/undone、配方和截图，素材与识别正文不提交仓库。本轮第二步开工声明 15 分钟检查上限；未到额度即完成验收，不继续扩展。未提交、推送或重启用户原应用。

## 2026-09-09：系统代理接入后真实调用通过，阻塞转为语音时间粒度

用户批准接入代理并复测。新增 cli-proxy-env：保留显式代理配置，否则在 macOS 每次翻译前读取 scutil 的静态 HTTP/HTTPS 代理；不固定地址或端口。将解析后的环境传给 Codex 与旧 CLI 的实际翻译进程，保留其他环境变量，不修改系统配置。PAC／SOCKS 和系统例外规则的转换未实现；读取失败沿用原环境。真实子进程测试先验证环境未传递导致失败，修复后相关测试 **81/81** 通过。

一次真实视频用例未在命令行设置代理，由产品自动读取系统配置。两次 Codex 均返回合法结果：首轮 15437ms 返回 prepare transcript，范围 0–17.6 秒；完成一次本地识别后，第二轮 27452ms 返回 clarify，要求更细的要点时间戳。识别结果只有一段 0–16.08 秒、40 字文本；现有 transcribe-subtitles.py 仅保留模型段级时间，未请求词级时间。

因此代理接入获得真实链路支持：首次请求→本地识别→带识别结果的第二次请求已通。完整用例仍 **1 失败、0 通过**，因为 AI 请求补充信息，未产生编辑，预览新效果／导出／撤销未执行。180 秒是测试等待执行成功的上限，不代表此次 CLI 超时。后续应评估细化识别时间信息，不要求用户手工制作 transcript；本轮不扩展修改识别器。

证据目录 `/tmp/srt-system-proxy-acceptance.ggwTbP/e2e/`，素材与识别正文不入库。本轮修复和日志尚未提交／推送，用户原应用未重启；真实验收使用独立 Electron 测试进程。

## 2026-09-09：显式代理连接验证通过

用户批准一次指定现有代理的 Codex 对照调用。复用生产 createLocalCliService、当前登录与提示词构建、JSONL 适配器和 60 秒上限，发送无视频内容的连接测试文字；仅此次进程设置 HTTP_PROXY／HTTPS_PROXY 及小写形式为现有系统代理 `http://127.0.0.1:1082`，额外使用只读沙箱，不修改持久网络或 CLI 配置。

结果成功：555ms thread.started、590ms turn.started、14234ms item.completed、14343ms turn.completed，14607ms 返回合法 clarify“连接测试成功”；直接调用进程已 close（完成后的 SIGTERM 收尾），未观察到重连事件。

与此前相同连接探针的无显式代理失败、视频验收的 TLS 握手中断，以及无认证 HTTPS 直连／代理差异一起，强烈支持“代理接入不一致”为主要阻塞方向。这是一次简单连接成功，不等于新视频完整链路通过，也不是同时进行的随机对照。正式应用尚未接入代理传递修复，本轮未改产品代码、重启、提交或推送。

## 2026-09-09：重连修复后复测，Codex TLS 握手中断

用户要求实测，在提交 `62c4152` 上沿用已授权的 17.6 秒视频与当前 Codex 配置运行一次真实桌面用例，零测试重试。结果 **1 失败、0 通过**；视频导入及正式预览 ready，首轮翻译服务耗时 60570ms，调用进程耗时 60019ms，达到现有 60 秒截止时间并 SIGTERM／close，返回 LOCAL_CLI_TRANSLATION_TIMEOUT。

捕获 thread.started、turn.started，以及第 2/5、3/5、4/5、5/5 次重连通知，均为 `stream disconnected before completion: tls handshake eof`。没有 turn.completed。重连通知不再提前结束请求，修复效果有真实记录支持；实际阻塞为连接过程的 TLS 握手中断。尚不能据此确定是代理、证书、网络链路还是服务端引起，未修改网络、认证或 TLS 校验。

翻译调用 1 次，本地识别 0 次，编辑 0 条；尚未发送识别文字、生成效果或导出／撤销验收。180 秒为测试断言等待，并非模型进程耗时。本轮没有改产品代码或追加调用。证据位于 `/tmp/srt-reconnect-acceptance.a6ny9y/e2e/`，不入 Git；研发日志尚未再次提交／推送。

## 2026-09-09：修正 Codex 重连通知被误判为终态

根据新视频验收实际捕获的 `error: Reconnecting... 2/5 (request timed out)`，修正完成适配器：error 事件本身不结束请求，等待 turn.completed／turn.failed、进程关闭或既有 60 秒截止时间。不依赖英文错误正文匹配，不重置截止时间，不添加应用层重试。收到 error 后若进程直接退出且无完成事件，仍拒绝；成功仍必须有最终回答与完成事件。

新增三个真实本地子进程回归：重连通知后成功、重连后 turn.failed、连续重连不延长截止时间。修复前成功场景和截止时间场景失败；修复后相关测试 **75/75** 通过，差异检查通过。调用进程清理维持原实现。

本轮未再次调用真实模型或运行视频验收，未重启应用、提交或推送。解决的是已复现的事件误判；真实网络请求超时原因及新视频完整链路仍未验证通过。

## 2026-09-09：新视频真实验收失败，发现重连事件误判

用户明确允许本地识别新视频并向当前 Codex 发送识别文字后，运行一次真实桌面用例，零测试重试。输入为用户提供的 17.6 秒、1280×720、30fps、有 AAC 音轨的 MOV；原文件未修改。测试导入转码副本，正式 Remotion 预览 ready，保存编辑前快照；范围校验改为使用实际素材时长。

结果：**1 失败、0 通过**。第一次翻译服务耗时 49190ms，Codex 子进程耗时 48670ms，记录 thread.started、turn.started，然后 error，正文为 `Reconnecting... 2/5 (request timed out)`。适配器收到 error 后 SIGTERM 结束进程，界面执行状态 not_run。180 秒为测试等待成功的断言上限，不是 CLI 实际耗时。调用 1 次，本地识别 0 次，项目编辑 0 条；未生成效果或导出文件，也未发送识别文字。

这项证据修正上一条“错误事件即明确失败”的判断：至少该 Codex 版本会使用 error 事件报告重连进度，将所有 error 一概视为终态会提前中断仍在恢复的调用。72 项自动测试未覆盖“重连通知后完成”，因此此前通过不能证明该分支正确。需进一步区分可恢复通知与 turn.failed／真实截止时间；底层请求为何超时仍未确定。此次不追加真实调用，不把失败归因于视频或 Remotion。

证据保存在 `/tmp/srt-new-video-acceptance.WFDAeT/e2e/`，含失败截图、事件摘要与项目快照，不提交素材或识别原文。验收脚本新增经过截断和 URL／令牌脱敏的错误摘要；本轮产品代码未修改，未提交或推送。

## 2026-09-09：Codex JSONL 完成机制修复

针对“模型已完成但 CLI 尚未退出”的误判，新增 `src/codex-translation.js`，由生产翻译入口调用 `codex exec --json`。只有 agent_message 与 turn.completed 同时具备才交付最终回答，仍由既有解析器验证业务内容。流式读取兼容分块 UTF-8；错误事件、缺失完成、损坏 JSONL 和输出超限拒绝。保留 60 秒任务截止时间，撤回此前 90 秒实验。Claude/Gemini 行为保持不变。

成功后给调用进程 250ms 自然退出宽限，随后 SIGTERM，再过 250ms 强制终止，等待 close 后返回。失败立即开始清理。JSONL 总缓冲上限 1MiB，单行 256KiB，最终回答仍为 64KiB；事件包装不占用原业务回答限额。本轮验证的是直接调用进程的结束，不宣称跨平台子进程树回收已验收。

- 测试先行：真实 Node 子进程发出完成事件后故意挂起并忽略 SIGTERM，旧实现在 1508ms 报超时；修复后约 538ms 返回且 PID 已不存在。
- 聚焦自动测试 **72/72** 通过，涵盖完成后挂起、明确失败、无完成事件、异常退出、输出损坏／超限、UTF-8 分块与超时清理，以及既有提示词／业务协议和其他 CLI 行为。
- 一次无视频的真实生产服务连接：当前 Codex 偏好，额外限定只读沙箱；486ms 收到 thread.started，520ms 收到 turn.started，38665ms 收到 error，38679ms 返回 LOCAL_CLI_TRANSLATION_FAILED，调用进程已 close（SIGTERM）。没有 turn.completed，因此真实成功路径未通过；本次错误正文未保留，无法从事件类型推断具体上游原因。没有自动重试、上传素材或改认证设置。
- 内部完成：协议适配与回归通过。用户可见成果：代码能根据明确事件结束调用；本次真实请求仍失败，不能宣称已经恢复模型正常回复。新视频的提炼、预览、导出与桌面完整验收尚待新素材。未重启用户应用、提交或推送。

修复计划：`docs/superpowers/plans/2026-09-09-codex-completion.md`。协议依据为 OpenAI Codex 非交互模式文档。既有真实视频测试改动保留，本轮不重复计算为新增成果。

## 2026-09-09：R5–R8 与 CLI 修复开发快照备份

用户明确要求先提交并推送最新版本。本次提交包含 R5 参考样式预览、R6 正式文字／玻璃样式、R7 图片／静音视频素材接入、R8 自动提炼流程代码、CLI stdin 修复及相关测试／计划／日志，推送目标为既有 `feature/subtitle-export-cycle0`，不合并主分支、不标记正式发布。推送前远端核对仍为 `e4dd71d`。

- 待提交 55 个文件经核对，均为源码、测试或文档；用户音视频、完整识别原文、凭据、会话日志和构建／测试缓存不入库。
- 共享 Remotion 场景构建通过。启用已有真实本地媒体开关的首轮 Node 回归 **451/452**，预览并发用例失败；该用例单独复跑亦失败。只读核对发现测试将异步完成后分配的会话编号当作请求顺序，期待关闭 `[1]` 而实际为 `[2]`；此前“旧请求取消、新请求成功”的断言通过。未借备份任务修改产品或测试。
- 同一代码、同一开关完整复跑 **452/452，零失败／跳过，12.487 秒**。这是顺序敏感测试的已知不稳定性，不因复跑通过就声称已修复。两次完整结果及单例失败均保留在 `/tmp/srt-github-backup.UZPn5o/`，临时产物不入库。
- 本轮没有重新调用真实模型，也没有复跑完整口播提炼／导出桌面验收。修后 CLI 简单连接已通过，但 R8 真实语义与成片验收仍开放；备份不改变这项状态。

## 2026-09-09：CLI 输入通道修复 — 生产入口连接验证通过

用户批准最小修复后，仅改 `src/local-cli.js`：保留 `execFile` 返回的子进程并结束其 stdin，移除不生效的 stdio 选项。60 秒／64KiB、argv、提示词、解析器、模型配置和 Remotion 均不变。测试先行：真实 Node 子进程等 EOF 的新回归先在 500ms 后因转换超时失败，修复后约 28ms 通过；替换了只检查 mock 选项的旧测试。

- **内部完成：** 输入生命周期修复和退出核查完成。最终 CLI／指令协议 **59/59**，零失败／跳过；另有真实子进程保护，确保“合法 JSON 后非零退出”与“合法 JSON 后超时”仍然失败，不把错误输出伪装成成功。独立聚焦审查无 Critical／Important，语法和差异检查通过。
- **真实连接结果：** 无视频内容的退出探针在 48,641ms 收到回复、55,008ms 正常 exit、55,009ms close；随后修后 SRT 生产服务／buildPrompt／解析器入口在 40,884ms 收到回复、46,109ms 正常 exit、46,110ms close，得到合法 `clarify: 连接测试成功`。两次均为实际 Codex 调用、无重试。生产入口探针不自行结束 stdin，由本次产品修复完成；诊断仅额外指定只读权限，未更改全局设置。
- **退出结论：** 本轮没有重现固定退出死锁，因此不增加新的完成协议或提前成功路径。上次边缘超时不能直接归为永久退出故障；本轮保留严格“进程成功结束后才解析”的行为。
- **剩余限制：** 启动／通信记录出现证书校验、WebSocket 重连及 MCP transport 错误类别，造成额外等待的可能性仍需单独核实；未查明具体证书链原因，没有绕过 TLS、换模型或修改用户网络配置。一次简单连接通过不保证复杂指令都在 60 秒内完成。
- **用户可见边界：** 已验证软件可接收和解析真实 CLI 回复；未重启用户当前应用，未复跑完整视频提炼、应用、导出或撤销。R8 完整语义验收仍未完成，不将连接测试当作视频效果交付。

证据 `/tmp/srt-cli-fix.gLw7YS/` 包含 exit-probe.json、production-probe.json、focused-tests.log；EOF 实施报告 `/tmp/srt-local-cli-stdin-fix.md`。本轮约 13:20–13:27 进行，30 分钟上限未耗尽；不精确核算交叠等待／开发百分比。没有提交／推送，也未改用户预览页面或另建监控系统。到此结束本次窄修复。

## 2026-09-09：Codex 超时窄诊断 — 确认输入通道未关闭

用户允许继续检查 CLI 启动／返回日志，本轮只诊断，不修改产品实现、全局配置或 Git。先确认 Codex CLI 0.153.4 可运行、显示 ChatGPT 已登录，再对比两个不含视频或识别文字的最小连接请求。保留原 60 秒／64KiB 上限、模型配置、工作目录与测试 PATH；两次诊断均额外限定只读权限，不把它们当作原视频全流程复跑。

**已确认缺陷：** `src/local-cli.js` 给 Node `execFile` 传入的 `stdio: ['ignore','pipe','pipe']` 实际不会生效，该函数内部未把此选项传给 `spawn`。子进程输入通道仍打开，代码又没有关闭它。Codex 会将管道输入当作额外上下文等待结束。无网络本地真实子进程对照为“不关闭：811ms 超时／SIGTERM；关闭：49ms 返回 EOF”；独立复核得到同一结论。既有测试只模拟成功并断言选项，未验证真实 EOF，因此漏检。

真实最小对照：原方式 **60,011ms** 被 SIGTERM 终止，stdout **0 字节**；只增加关闭 stdin 后 **173ms** 已出现模型初始化信息，**56,756ms** 收到合法 `clarify` 回复“连接测试成功”（50 字节），但进程到 **60,014ms** 仍未结束，被上限终止。对应 Codex 会话有任务完成记录。适配器先因子进程错误拒绝，未解析已收到的回复，这是另一个已观察到的失败环节；具体为何返回／退出耗时较长尚未确定，不能说关闭 stdin 就已修好全链路。

诊断脚本最初将不存在的 `error.code` 用 0 代替，两个历史 JSON 内的 `code:0` **不代表退出成功**；应结合 `killed:true`、`signal:SIGTERM` 读取。脚本已纠正后续记录方式，没有重新请求模型或改写历史观察。本次未上传视频、未改变提示词／模型／渲染器，未验证语音提炼、应用、导出或撤销。

内部结果：定位输入生命周期缺陷，并确认解阻后能产生真实回复；用户可见成果仍为诊断结论，产品尚未修复。最小后续应修正输入关闭、补真实子进程回归，再检查完成与退出处理；不增加重试平台、不盲目延长超时、不在任意进程错误后直接接受部分输出。证据 `/tmp/srt-codex-diagnosis.J6fwj3/FINDINGS.md`、`baseline.json`、`close-stdin.json`；临时文件不入 Git。行为依据：[Codex 非交互调用说明](https://learn.chatgpt.com/docs/non-interactive-mode)。

## 2026-09-09：R8 切换 Codex CLI 对照测试 — 转换阶段仍超时

用户要求“换成 Codex 的 CRI 尝试一下”，按 Codex CLI 执行。通过生产选择接口把 SRT 偏好切为 `codex`，结束时重新读取确认仍为 Codex。只切换 CLI，保留原提示词、60 秒调用上限、解析规则、Remotion 和模型自身配置；未读取或改写认证凭据。

- 13:02:46 开始一次真实 opt-in E2E，零重试，约 13:05:53 结束。使用同一参考片前 35 秒原声与干净底片，不上传视频。界面根据真实偏好选择 Codex；生产翻译前再核对界面选择与真实偏好一致，防止此前固定 Claude 按钮造成显示不一致。
- **结果：1 失败、0 通过，退出码 1。** 等待状态仅观察到 waiting → not_run，截图为“编辑指令转换超时，请重试”。尚未观察到语音准备阶段，未获得可应用配方，未执行后续导出／撤销断言，没有新重点文字或成片。
- 180 秒是测试等待完成的断言上限，不是测得的 CLI 运行时长。本次没有可持久读取的原始退出码／标准错误，也未找到精确匹配的新增 Codex 会话，不能判断上游是否收到请求、实际后端模型或具体超时根因。不能凭本次结果认定 Codex 模型本身故障，也不能认定 Remotion 渲染失败。
- 本轮只修改真实测试入口：选择一致性检查、过程元数据采集及失败证据保存。首次使用 `testInfo.attach(body)`，但当前 line 报告器没有将 JSON 正文落盘，导致本次内存证据未保留；这是测试记录缺陷。已改为先写临时 JSON 再附件引用，并以离线 hook 检查证实落盘，不伪造本次已经丢失的数据，也不为补日志擅自再调用模型。
- 本轮 CLI 单测 **21/21**；两份测试脚本语法检查和 `git diff --check` 通过。上述离线检查不代表真实链路通过。独立只读审查核对了调用来源、失败截图和证据限制。

内部结果：已切换默认 CLI 并完成一次对照尝试；用户可见结果：转换阶段失败，未生成效果。R8 真实 AI 完整验收仍未通过；下一步应检查 CLI 启动／返回错误，而不是重做卡片或渲染器。证据目录 `/tmp/srt-r8-codex-acceptance.rBmv1G/` 保留 run.log、失败截图、错误上下文及本地测试底片，不纳入 Git。没有追加真实请求、延长超时、修改产品代码、提交或推送。

## 2026-09-09：R8 真实模型联调 — 第二轮超时，验收未通过

用户在本轮明确以“可以”授权：把参考视频前 35 秒的识别文字发送给当前 Claude CLI，运行一次真实提炼测试，不上传视频。12:40 开始，使用独立 Electron 测试项目；画面为干净底片，声音为参考片前 35 秒，避免将原片烧录特效当作新生成结果。现有 opt-in 用例仅运行一次，`--retries=0`，没有自动追加请求或更换模型配置。本轮没有修改产品／测试代码，没有提交／推送。

### 已确认的真实执行与失败位置

- 实际资料目录的 `local-cli.json` 为 `selectedCliId: claude`；测试页面 CLI 列表／按钮仍是隔离 fixture，真正翻译使用该资料目录和生产 `createLocalCliService`、`buildPrompt`、CLI 子进程与严格解析器。本地识别使用生产服务，未用固定转写文本。
- 对应首个 CLI 会话日志：12:40:57.800 收到请求，12:41:06.151 返回合法 `prepare`，资源为 transcript、范围 0–35 秒。这证明首轮连接、准备协议与解析可工作，不支持“调用链完全没连通”的判断。
- 第二个会话在 12:41:16.843 收到包含完整 transcript 的请求：`source=speech-recognition`、12 段、覆盖 0–35 秒、`complete=true`。这是本次实际识别结果；上一轮独立 WAV 识别的 21 段不能代替本次数据。字幕正文未写入仓库。
- 第二个会话没有已记录的助手输出。失败截图显示“编辑指令转换超时，请重试”，执行状态 `not_run`；测试观察到 waiting → generating → not_run。结合代码路径，失败在语音准备后的第二次真实翻译，尚未进入应用与 Remotion 导出。
- CLI 日志首轮响应的模型字段为 **`deepseek-v4-pro`**。这是现有 CLI 配置所返回的模型标记；Claude CLI 入口不等同于 Claude 模型身份。未据此更改用户配置，也不把该标签当作服务商底层模型身份的独立证明。

### 结论与证据边界

真实 E2E **1 失败、0 通过，退出码 1**。180 秒为测试等待“执行成功”的断言上限；产品 CLI 调用上限仍是 60 秒，另有 64 KiB 输出缓冲限制。当前适配器把子进程 `killed` 或 `ETIMEDOUT` 映射为超时，没有保留这次原始退出原因，精确会话的 debug 文件也不存在。因此可以确认失败阶段，尚不能断言是模型生成慢、网络等待、服务端问题或输出限制导致；未提高超时、放宽校验或构建监控平台。

没有本次生成的卡片／成片，也没有执行后续导出、画面质量和撤销断言；失败后未另外读取已清理的隔离项目快照，不能将未执行的断言算作通过。内部进展：真实首轮准备、本地识别、原文注入第二轮已有证据。用户可见结果：明确失败状态，尚未获得 AI 生成的重点文字视频。R8 完整验收仍未通过，与此前固定边界下的本地通过结果分开记录。

证据目录 `/tmp/srt-r8-real-acceptance.xTpVLy/`：`run.log`、不含原文的 `evidence.json`、测试失败截图及错误上下文。两份精确任务匹配的本地 CLI 会话号为 `26b76e1d-c5ea-4db7-8533-35aa45b7a466` 与 `21de029a-f3fb-48c5-9b52-76cc77f733ab`；只读取本任务记录，不复制 CLI 推理块或凭据。独立只读审查核对生产调用来源及失败结论；具体截图和临时音视频不入 Git。

下一步应窄查第二轮 CLI 的真实耗时／退出原因及配置，不重做已验证的卡片与 Remotion 链路。再次真实调用或调整实现另待用户指令；本次授权已用于这一轮，不自动重复消耗。

## 2026-09-09：R6–R8 — 正式样式、素材卡片与要点提炼接入

用户已认可 R5 的卡片、大标题／说明／标签外观，并授权继续正式接入与自动提炼。本轮沿用 Remotion 和原界面配色，在 `feature/subtitle-export-cycle0` 的既有工作树内开发；不提交、不推送。

### 内部实现与用户可见成果

- 正式文字／底板支持字重、字距、轻阴影、局部背景模糊和边缘光；参数进入现有能力目录，由 AI 组合，不新增效果名查表。共享 R5 绘制组件，保持旧字段中性默认、原存档加载不写回。一个玻璃卡片一个组合，玻璃底板为该组第一个子层；多个卡片使用多个组合。
- 编辑器原输入框旁增加轻量“导入素材”和素材选择入口。PNG、JPEG、WebP、MP4 使用项目内稳定素材 ID；路径留在主进程。卡片可放清晰图片或按入点播放的静音视频，沿用指定时段、淡入／缩放等动画；预览／MP4 同场景，保留主视频声音。
- 新素材引用会重新准备预览资源集合，切换完成后才释放旧会话；保存、重开、一次请求整次撤销和正式 MP4 已实际通过。文件移动时，应用、重开预览及导出会明确提示素材不可读取；还原原路径后重新打开可恢复，不删除项目编辑。
- 提炼请求优先使用完整已应用字幕；没有时允许模型返回一次 `prepare` 请求，本地识别后续推一次。原文保留原时间，超出 24,000 字符时明确缩小范围，不冒充完整文本；读语音不添加字幕轨，也不应用／覆盖字幕草稿。对话反问后复用同一请求的临时原文；最终一次提交全部定时文字组，各组在时间线上保留实际区间与间隙。
- 准备前固定项目、主素材与版本；处理期间发生变化则不提交过期结果。成功状态自动折叠、整次撤销和重开不重复执行沿用既有机制。没有新增队列平台、任意代码执行、云素材、跨项目自动素材重绑定或人脸跟踪。

### 验证性质与未完成项

- 全量 Node 检查启用既有真实 Remotion／Player／FFmpeg 开关：**450/450，零失败、零跳过，16.241 秒**；日志 `/tmp/srt-r6-r8-Tyoxuo/node-final.log`。此前沙箱禁止本地端口造成的 `EPERM` 与旧配方默认值兼容失败分别记录和处理，没有把环境失败冒称产品通过。最新共享场景构建通过。
- 桌面固定模型／识别边界下，要点生命周期 **8/8**，覆盖已有字幕、草稿保留、无字幕、无语音、重复准备、越界、处理中版本／主素材改变、反问后回答；随后首例增加真实 MP4 导出并通过 **1/1，7.0 秒**。正式媒体与旧 R4 卡片检查 **2/2，20.0 秒**；素材移动、导出／重开提示、还原恢复的强化媒体用例 **1/1，8.3 秒**。这些结果不相加冒称独立用例总数。
- 图片与视频导入经过真实文件探测和项目素材索引；操作经过实际编辑器、IPC、项目事务、Player 与 Remotion 导出。外部模型／ASR 流程测试使用固定响应，原生文件／保存对话框由测试返回路径，不冒称用户人工操作或真实 AI 理解。
- 另用生产本地识别服务读取参考片前 35 秒，返回 **21 段**。参考画面没有用于新效果证据；测试成片使用干净底片。导出音频抽样为主视频 **880Hz**，内嵌视频 **220Hz** 未混入。
- **真实 Claude 语义提炼未执行。** 权限审查要求用户明确同意向当前选中的 Claude CLI 发送参考片前 35 秒的识别文字；已提出授权请求，未绕过或经其他代理发送。视频文件不会上传。已有字幕／无字幕的本地流程通过不等于真实模型提炼质量、时段与美术表达验收通过，因此 R8 最终验收仍开放。
- 独立审查发现并推动修复了素材索引异常时静默覆盖、旧导出默认字段兼容、反问后原文丢失和错误提示等实际问题。最终长字幕选段反问衔接只调整缓存恢复的条件：完整已应用字幕不可用时才恢复当前请求的已选原文，保留身份绑定和范围检查。新增 400 段真实项目字幕的用例先以第三次调用缺少原文失败，再通过；最终相关桌面 **9/9（34.2 秒）**、协议单测 **41/41**。这是全量 450 项之后的窄 UI 修复，未再次重复全量测试。没有因此改变默认渲染核心或增加通用重试系统。

### 证据与边界

稳定临时证据目录 `/tmp/srt-r6-r8-Tyoxuo/`：`formal-media-cards.mp4`、`formal-image-card.png`、`formal-video-card.png`；`fixed-keypoints.mp4`、`fixed-keypoints.png` 明确是固定测试内容，不是真实 AI 摘要。临时视频、识别文字、素材索引及用户路径不纳入 Git。原 R5 预览保持可访问。

内部状态：R6/R7 正式功能与本地闭环已接通；R8 实现与固定边界验收已接通，真实模型验收待授权。用户可见：正式卡片、图片／静音视频、可播放 MP4 和定时文字流程。尚不承诺自动理解无语音画面、精准避让人物、任意素材跨项目重绑定、已有卡片原位修改或 Windows／安装包已验收。

北京时间 11:50 开始，12:06 首个正式编辑器可见闭环通过，早于 12:35 半程检查；后续完成本地识别、提炼流程和回归审查。R8 不因等待授权自动延长范围。时间包含并行开发及工具等待，未逐项精确计量，不宣称支撑占比已精确核算。

明确延期的非阻断限制：一批导入中前几个文件成功、后一个失败时，已成功项仍保存在索引，但当前列表需要重新打开编辑器或下一次成功导入才刷新；普通单窗口按钮有忙碌保护，但底层 API 的同项目并行写入尚未串行化。本轮不为这两项建立队列或事务平台。原素材文件不复制进项目，移动文件后须还原路径，或重新导入并重新生成卡片；不承诺自动重新绑定旧卡片。

**12:35 收尾：约 45 分钟。** 独立整体审查与 P2 聚焦复审通过，剩余零 Critical／Important；上述两个 P3 明确延期。最终 `git diff --check` 通过，HEAD 仍为 `e4dd71d`，全部本轮改动保留在工作树，未提交／推送。此处结束本地开发，不扩项；只有真实模型文字发送授权尚待用户决定，R8 最终验收不标记完成。

## 2026-09-09：R5 — 重点文字与粉色毛玻璃卡片视觉试做

用户要求简化开发计划后批准执行。仅推进第一步，沿用 Remotion；新增 `GlassSurface` 与 `StyledText` 共享绘制组件、人工配置的两个场景和薄预览/渲染入口。正式编辑器、AI 能力目录、素材管理、项目保存格式未改动，不把这次试做当作正式接入完成。

**用户可见：** 两段真实 MP4（画面 8 秒、14 秒，1280×720、30fps、H.264/AAC）与同场景 Remotion Player。玻璃卡片具有粉色透明填充、局部背景模糊、圆角、柔和边缘光、整体淡入淡出/轻缩放；前 4 秒展示图片，后 4 秒展示静音视频。文字片段含两组错时出现的大标题、说明及标签，文案和时间为人工配置。

**内部验证：** 新组件及动画采样先失败后实现；最终定向 19 项通过、0 失败、1 项既有浏览器驱动测试未启用。实际渲染对照验证普通视频/已调色 Canvas 的后景模糊，入场与停留、图片内容清晰。内置浏览器验证播放暂停、向后和向前拖动、内嵌视频静音与相对入点、暂停 Canvas 帧、第二组文字出现；无页面脚本错误，保留 Remotion 自带许可提示，不将其伪称为测试故障或擅自确认许可。独立只读代码审查无阻断问题。

**投入与边界：** 北京时间 11:22 开始，约 11:32 生成两段首版，11:35 生成修订版，11:44 完成主要核验，未达到 60 分钟止损线。工具/权限与薄预览入口的支撑投入粗估 4–5 分钟，非精确工时。两侧使用演示背景避免原片烧录特效混入新成果；中间原片既有文字不是本轮生成。还未做自动提炼、正式素材导入或 AI→项目→导出完整链路；用户观感待确认，后续阶段需据本轮结果校准，不以试做耗时外推全部开发时间。

产物：`/var/folders/dz/dz1x73p96xn6d1ss30xqk1dw0000gn/T/srt-r5-u2MwLr/`，包含两段 MP4、入场/停留/退场帧及普通视频/Canvas 的模糊对照；临时媒体不入 Git。当前本地预览 `http://127.0.0.1:58660/`，脚本进程保持运行供用户查看。本轮没有提交或推送 Git。

## 2026-09-09：Remotion R4 — 通用卡片底板样式

### 用户可见成果与实现边界

用户确认以参考视频约 02:11–02:24 的信息卡为依据，只增加通用底板样式。现有 `visual.shape@1` 新增四个可选参数：圆角半径、内描边厚度、描边颜色和填充透明度；半径与厚度相对于实际底板较短边。独立矩形与组合子层共用同一个 Remotion `VisualLayer`，预览和实际导出使用同一实现。填充透明不使文字或描边变淡，既有组透明度仍整体合成。

四字段自动进入既有能力目录、提示词和严格解析；没有新增能力 ID、效果模板或第二条渲染链。缺省保持旧矩形外观，旧项目/撤销快照加载不写回。现有保存重开、最近一次请求整次撤销与个人技能继续保留样式。第二请求验收是在另一时段新增不同文案/样式，不代表已支持原位修改旧卡片。

产品仅改 `src/visual-layers.js`、`app/remotion/layers.jsx` 及三个必要的历史入口兼容/保护文件 `src/render-recipe.js`、`src/video-export.js`、`src/visual-group-export.js`。没有修改 UI 配色、布局、依赖锁文件、模型协议、存储版本；图标、阴影、字体系统、图片、画中画、新动画或自动排版均未加入。R4 相对开工快照 `/tmp/srt-remotion-r4.bX0Rwi/baseline/` 审查，保留全部未提交 R1/R2/R3，不把这些既有变化算成本轮新增。

### 实际失败、必要修正与审查

- 测试先行验证新字段原先被拒绝；旧数据测试最初缺少原有必需字段，修正为真实编辑链生成合法文档后只删除新增字段，不把错误 fixture 计作产品缺陷。
- schema 扩展确实使历史五字段 shape recipe 被误拒绝；只补旧字段集合的兼容验证。历史 Canvas/FFmpeg 不再静默把新卡片降级为实心方块，而是明确拒绝可见的新样式，没有另造渲染器。Task 1 定向最终 113 通过、1 个可选浏览器跳过；历史入口定向 29 通过、3 个可选真实媒体跳过，均由后续全真实测试覆盖。
- 桌面固定场景先因未接入测试翻译分支而失败，最小接入后通过。独立审查指出音轨仅检查存在、技能仅检查写入，补充了原合成声音识别及重开后的“我的技能”查看/选择。音频测试曾因本机 FFmpeg 不接受 `.5` 时长而失败，只改测试参数为 `0.5`；最后定向 **1/1，13.2 秒**。
- Task 1、Task 2 复审及全轮产品审查均通过，无未解决问题；独立审查只读，没有覆盖或提交产品改动。范围没有扩展到音频系统、技能平台或通用测试框架。
- 开工基线曾出现既有 `main-entry.test.js` 异步顺序断言抖动；独立诊断及未改代码复跑已记录在 R4 计划中，未计作 R4 修复。最终全真实复跑未出现该失败。

### 最终验证与可见证据

- 主代理启用全部真实媒体/Remotion 开关运行 Node 测试：**418/418，12.883 秒，零失败、零跳过**；日志 `/tmp/srt-remotion-r4.bX0Rwi/node-final.log`。
- Remotion 桌面回归 **16/16，1.8 分钟**。补强音频和技能断言后，最终完整桌面回归 **83/83，5.3 分钟，零失败、零跳过**；其中 16 项为 Remotion，67 项为显式隔离的旧路径回归。不能把后者冒称全部在新引擎执行。日志 `e2e-final.log`，产物位于同临时目录的 `e2e-final/`。
- 最终 R4 横版 640×360、竖版 360×640，各为 H.264、24fps、96 帧、视频 4.000 秒、AAC 音轨 4.010667 秒。两份都由真实桌面导出按钮生成；音频解码确认分别保留 880/990 Hz 的非静音原合成声。实际 Player 和 MP4 的圆角外侧、填充、边框、透明内区及其边框共五点按既有 12/255 容差对照，不放宽阈值。
- 已验证旧存档不写回、新请求追加/保存重开/整次撤销、技能保存并重开查看/选择、白字不随底板变淡；主代理查看了两种画幅最终预览和成片抽帧。固定测试翻译经正式解析、项目状态、Player 和导出链路，不代表真实 AI 已完成推导。屏幕颜色解释和字体抗锯齿沿用既有明确限制，不声称屏幕截图与编码帧逐像素相同。
- 易于观看的四秒演示为 `/tmp/srt-remotion-r4.bX0Rwi/r4-card-style-preview.mp4`：1280×720，四张带不同色边框的卡片依次出现、共同淡出，保留音轨。它是固定配方的真实 Remotion 服务成片，不是产品内置模板，也不是参考片逐像素复刻。正式桌面横竖版证据在 `e2e-final/remotion-card-style-flow-R-1309b-cards-in-both-aspect-ratios/`。

### 投入与停止条件

17:26:29 UTC 开工，17:39 已生成并查看首份真实成片，早于 18:11:29 半程检查；18:08 最终完整桌面回归结束，随后只做记录和差异核对，未接近 18:56:29 的 90 分钟止损线。约前 5 分钟为准备/基线，之后产品、必要兼容修正、桌面验收和独立审查并行；工作与等待重叠，不虚报精确有效工时或支撑占比。

本轮按测试先行和独立审查完成已批准的本地功能。真实 AI 最多两次联调的本轮问题尚未获得明确答复，**没有调用模型，没有上传参考视频/截图/私人字幕**；该验收门仍待授权，用户参考观感及 Windows/安装发行版仍未验收。当前保留工作树，未提交或推送 GitHub，不自动继续扩展。

## 2026-09-09：Remotion R3 — 现有八能力统一预览与成片

### 成果与边界

用户以“继续R3”批准本轮。保留全部 R1/R2 未提交成果，先快照触及目录，再按 `/tmp/srt-remotion-r3.TvI79k/baseline/` 审查本轮差异，而非把 Git HEAD 后所有改动算成本轮。内部九项计划验收完成；现有调色、翻转/中心缩放、颗粒、暗角与矩形、文字、动画组、字幕，全部进入同一 Remotion 场景。新增源画面绘制沿用现有颜色矩阵、整数变换几何与确定性纹理规则，按图顺序、帧时间和半开区间执行；覆盖图层不被源效果误调色或翻转，没有另一套随机数或动画时钟。

能力完整性改为要求四个编辑核心转换，并另查实际 renderer 支持集合；只有核心适配器且受场景支持的注册项可执行，没有实现的节点不发布给 AI，历史声明仍能读取。正式入口默认 Remotion，未知图明确失败，不静默回退旧路径。AI 提示词/协议、本地 CLI、项目事务、个人技能实现及依赖锁文件相对 R2 均未改变；没有增加预设效果或未来能力。

检测页现核查包版本、本地两类构建、已安装浏览器实际渲染固定本地页面、媒体探测工具，并将结果接到正式预览/导出；不再依赖旧 FFmpeg ass 滤镜判定 Remotion 是否可用。浏览器必须已有且版本匹配，无隐式下载。沿用已有有时限进程执行器和临时目录清理，没有新增进程平台、队列或缓存框架。

### 真实失败与修复

- 测试先行覆盖缺失绘制、核心适配器准入、默认路径拒绝旧 recipe 及旧 ffmpegPath 门槛；定向通过后才进入桌面。真实源媒体测试曾因测试 HTTP 未支持 Range 而取不到新帧，修复测试服务，未把它当产品 RED。实际绘制曾因回调身份变化触发旧 blob 解码警告，稳定回调后通过正常播放、反向 seek、暂停修订和绘制失败传播检查。
- 首次完整桌面保护检查 4 通过、2 失败：暂停新增调色时主进程原生异常框显示 `FileHandle.createReadStream fd=-1`。用电脑窗口读取异常，再以取消请求后重复/并发 Range 的单元测试复现。原因是 response 销毁流会关闭共享 FileHandle；改为响应借用固定数字句柄且不独自关闭，会话等待所有 reader 结束后唯一关闭文件。RED→GREEN **6/6**，原卡住桌面操作随后通过，未改源文件保护或扩大资产访问。
- 另一断言错把首条字幕 0.2 秒写为第 0 帧；修正为 24fps 下第 5 帧。混合横竖屏初跑在最后切项目时把 `/tmp` 与规范化 `/private/tmp` 当作不同文件；只修测试 realpath 期望。两类错误没有靠延长超时或修改产品语义掩盖。混合与保护最终定向 **8/8**（45.9 秒）。
- 独立审查确认浏览器缓存查找错误依赖 cwd，以及 Remotion BrowserRunner 等待调试端点失败的清理缺口。缓存改按应用依赖根目录定位，探测改为已有 runner 执行固定 data URL 后退出、finally 清理独立 profile；从 `/private/tmp` 启动的真实 Node 和 Electron 运行时都能定位已安装浏览器并通过。复审无待修 Critical/Important；只验证已有 runner 的直接子进程终止，不宣称任意后代进程树或跨平台安装包已验收。

### 最终验证与可见证据

- 主代理最终 `npm test` 启用 `SRT_REAL_REMOTION_SOURCE=1 SRT_REAL_REMOTION_PLAYER=1 SRT_REAL_REMOTION=1 SRT_REAL_EXPORT=1` 及已安装 FFmpeg/ffprobe 路径：**410/410**，12.753 秒，零失败、零跳过，包含实际 Player 与 Remotion MP4。
- 最终 `SRT_REAL_EXPORT=1 npm run test:e2e -- --workers=1 --output=/tmp/srt-remotion-r3.TvI79k/e2e-final`（同时设置本机工具路径）：**82/82**，5.1 分钟，零失败、零跳过。脚本现在自动构建并包含所有 Remotion 用例；其中 15 项覆盖 Remotion，67 项保留显式禁用新引擎的旧路径回归，不能把后者说成全部在新引擎验收。
- 本 Mac 真实环境页另跑 **1/1**（1.7 秒），检查实际 `modes.remotion=ready`，无安装行为。`git diff --check` 通过；进程只读核对未留下本轮 Electron/渲染浏览器/测试运行器。
- 横竖屏完整流程为实际聊天输入（明确固定配方，非真实 AI）→八步整次应用→播放/反向跳转→字幕保存/应用→技能保存/查看/选择→重开→真实 MP4→取消/再次导出→整次撤销→换项目。底层存档、草稿和事务机制沿用原实现，原界面颜色与布局未改。
- 最终目录下 `remotion-source-effects-fl-47943--undoes-all-eight-abilities/`（横版）及 `remotion-source-effects-fl-bd71e--undoes-all-eight-abilities/`（竖版）含四份 first-export/successful-reexport MP4、预览及抽帧。主代理独立探测：640×360 / 360×640，H.264（探测 yuvj420p）、24fps、96 帧、视频 4.000 秒；横版单 AAC 48kHz 音轨与容器 4.010667 秒，竖版没有音轨、容器 4.000 秒。已查看最终两种画幅的实际画面，中文完整可读；不是参考视频复刻或外观像素级一致性保证。
- 源效果检查覆盖时间标记、区间边界、变换先后与裁剪、黑边参与后续调色、噪点同帧确定性/区域统计、暗角和不受源效果污染的覆盖层。纯色比较仍以统一 sRGB 内容、12/255 通道容差检查，未放宽；不以屏幕 ICC/字体抗锯齿差异掩盖误差。

### 投入与剩余事项

16:21:55 UTC 开工，16:52 前已有横竖版可见混合预览和成片，早于 18:51:55 的半程检查；17:00 完整桌面回归结束，约 17:03 完成记录，墙钟约 41 分钟，未用满五小时上限。准备与基线约 3 分钟，随后产品接入、现有环境检测和验收并行，约 16:40–16:54 处理真实集成问题，16:55–17:01 最终回归/核验；劳动和等待交叠，未精确分摊有效工时或支撑百分比。没有额外基础设施项目，不为消耗预算继续添加边界测试。

按实施、测试先行、独立审查及完成前验证流程推进，实际失败决定必要修复范围。R3 到此停止，保留工作区而不自动提交/推送；原有 R1/R2 与本轮结果均仍在 `feature/subtitle-export-cycle0`、HEAD `38c6263` 后的本地改动中。真实 AI 未调用、私人视频未上传，第七周期真实 AI 竖版超时仍未解决。R1 字体实际命中/屏幕色彩解释限制、用户个人素材和原生保存框人工体验、Windows/安装发行版仍待验收；新增 app/src 文件受既有打包通配规则覆盖，但这不证明浏览器已正确分发到安装包。R4 等参考视频后另定一个必要能力增量，不自动扩项。

## 2026-09-05：Windows / macOS 环境检测链路

### 本轮目标

在不重写编辑器的前提下，让同一个 Electron 环境页正确识别 Windows 与 macOS，显示视频剪辑所需的硬件和工具状态，并在用户明确确认后执行受限的安装动作。

### 用户可见成果

- 环境页会自动识别 Windows 或 macOS，展示芯片、CPU、内存、磁盘、图形能力和必要工具。
- 检测结果区分“满足”“可用但受限”“不满足”及具体失败原因，不再把探测失败等同于未安装。
- 硬件结果只做风险提示，用户始终可以继续进入主页和编辑器。
- 缺少工具时先展示安装计划，支持取消或确认；成功后自动重新检测，失败时显示手动处理指引。
- macOS 从源码运行时能识别 Homebrew 的 Node.js、npm、Python 3、FFmpeg 与 Metal 能力。

### 关键安全边界

- 页面只能请求白名单工具，不能提交或执行任意命令字符串。
- 每份确认仅绑定一个工具且最多使用一次。
- 取消不会触发安装；安装失败不会被记为成功，也不会自动重试。
- 开发服务仅绑定本机，不再对页面公开通用命令执行或通用安装入口。
- 安装结果不返回令牌、完整环境变量或完整诊断日志。

### 验收缺口修复

- 修正 Windows 捆绑工具的实际文件大小元数据生成，避免发布包因占位值而误报工具不可用。
- 补齐 macOS 版本化 Homebrew 路径的工具探测，使安装后的全量重检能看到 Node.js、npm 和 Python 3。
- 将 Windows 捆绑 Python 纳入报告，并让 npm 缺失时进入 Node.js 修复计划。
- 区分 Whisper 确实未安装与探测过程出错，保留可核对的失败原因。

### 最终验证

- 单元与集成测试：**91/91 通过**。
- Electron 端到端测试：**5/5 通过**。
- 当前 Mac 真实环境只读烟雾测试：**1/1 通过**。
- macOS arm64 目录构建：**通过**。
- 最终整枝审查：**Ready to merge: Yes**。
- GitHub 集成：[Pull Request #1](https://github.com/trydays/srt/pull/1) 已通过普通合并提交进入 `main`，仓库默认主页已展示本轮新版本。

> 上述数量是 2026-09-05 的验收快照，会随测试集演进；README 不依赖这些易过时数字。

### 明确延期

以下工作不属于本阶段验收，不阻断集成：

- 在真实 Windows 设备上运行烟雾测试。
- 可选 CUDA 能力检测。
- macOS 签名、公证和正式发布。
- 通用 helper / policy 重构与跨项目平台化。

## 2026-09-05：本地 CLI 检测与选择

### 本轮目标与用户可见成果

只扫描固定的 Codex CLI、Claude Code、Gemini CLI。环境页只显示成功检测到的项；没有结果时精确显示“未扫描到可用本地 CLI”。用户可明确选择一个全局本地默认值；系统绝不自动选择或切换，原选择消失时会清除。该能力始终可选，不会阻止“继续”进入主页和编辑器。

### 实现边界与范围控制

- 仅使用固定 `--version` 短探测和窄 Electron IPC/preload 的读取、重扫、选择方法；不新增任意 CLI 调用入口。
- 扫描、选择、两条 Electron 场景、独立审查及修正均以远低于原先拒绝的 4–8 小时估计的工作量完成；未新增平台框架或无关重构。

### 最终验证

- 单元测试：**98/98 通过**。
- Electron 端到端测试：**7/7 通过**。
- 当前 Mac 真实环境只读烟雾测试：**1/1 通过**；实际扫描到 Codex CLI 与 Claude Code。
- 最终独立审查：**Ready to merge: Yes**。

### 明确延期

调用所选 CLI、自然语言到精确剪辑指令转换、Remotion 执行集成、CLI 安装/登录/模型控制、更多 CLI 或自定义注册表、真实 Windows 烟雾与平台级验证，以及偏好文件规范化和重启级持久化覆盖，均留待后续独立阶段。

## 2026-09-06：自动字幕真实运行链路

### 本轮目标与用户可见成果

在不建设通用编辑框架的前提下，打通“给视频加字幕”从本地 CLI 指令识别、固定 Small 模型本地转录，到单字幕轨、画面时间同步、文字编辑、项目持久化、重新生成和一次撤销的完整纵向链路。

### 实现边界与范围控制

- 只支持固定字幕指令、单字幕轨和应用管理的 `faster-whisper` Small 模型。
- 未扩展多轨时间轴、字幕块拖动或时间调整、字幕样式、翻译、说话人识别、云端识别或导出。
- 未建设通用事务、重试和供应商适配框架，也未借本阶段重构无关旧代码。

### 最终验证

- 产品实现基准：`feature/auto-subtitles-runtime` @ `ff80066`。
- 单元与集成测试：**123/123 通过**。
- Electron 端到端测试：**19/19 通过**。
- 当前最终版本真实 Mac 复跑：已选 Claude Code 将自然语言识别为字幕操作；10 分 55 秒中文视频两次均由本地 Whisper Small 生成 **315 条字幕**。
- 视频播放到不同时间点时画面字幕同步变化；首条文字修改为“最终复跑：字幕编辑已持久保存”后刷新仍保留；再次生成后，唯一一次撤销恢复了该修改版。
- 复跑期间没有下载模型、没有触发安装、没有生成导出文件；结束时没有残留 CLI 或转录子进程。
- 最终整枝审查：**未发现 Critical、Important 或 Minor，Ready to merge: Yes**。

## 2026-09-07：字幕视频导出真实整链验收

### 本轮目标

把周期 1 的 FFmpeg 渲染与周期 2 的编辑器接入串成真实可用链路，并跑冻结回归，确认“给视频加字幕 → 导出烧录 MP4”达到最低可用。

### 用户可见成果

- 编辑器右上角“导出视频”入口走系统原生另存为，把已应用字幕烧进 MP4。
- 导出期间显示真实进度、可取消，仍可播放 / seek；字幕修改未应用时会阻止导出。
- 导出全程保护源文件与已有目标文件，不覆盖。

### 实际实现

- preload 暴露 `resolveVideoSource`、`startVideoExport`、`cancelVideoExport`、`onVideoExportProgress` 四个窄方法。
- 主进程只维护一个普通 `activeExport` slot，覆盖对话框、路径处理、进度转发与失败码收口；无 Job 类、队列或任务中心。
- 页面只维护一个 `window.isExporting`，冻结写入口；无通用锁或事件总线。
- 渲染服务固定 MP4 / H.264 / AAC / yuv420p，用 libass 烧录白字 + 0.72 半透明黑底。

### 最终验证

- 冻结回归：`node --test tests/environment.test.js tests/render-recipe.test.js tests/video-export.test.js tests/main-entry.test.js` → 56 通过 / 0 失败 / 1 条件跳过。
- 编辑器 E2E：`npx playwright test tests/e2e/video-export-flow.spec.js tests/e2e/auto-subtitles-flow.spec.js` → 9 通过 / 0 失败（30.9s）。
- 真实渲染（ffmpeg-full 9.0.1，`/opt/homebrew/opt/ffmpeg-full/bin`）：
  - 横屏 640×360，H.264 + AAC，4.0s，字幕区 `YMAX=236` 证明白字已烧录。
  - 竖屏 360×640，H.264 + AAC，中文长句烧录（`YMAX=242`），音频保留。
  - 真实取消：返回 `cancelled`，无伪产物文件，源文件字节数不变。

### 明确延期 / 限制

- 真实 GUI 手动整链（导入真实视频 → 本地 CLI 识别 → Whisper 生成 → 点导出 → 外部播放器人工确认）仍需在真实窗口点验；本周期用真实 ffmpeg 渲染与自动化 E2E 分别覆盖渲染与交互，未重复人工矩阵。
- 带显示矩阵旋转元数据的手机竖屏：官方 ffmpeg 无法生成忠实夹具（`-display_rotation` 只会物理旋转、不写矩阵），已用物理 360×640 竖屏端到端验证；该元数据路径仅由 `probeMedia` 维度计算覆盖，留待真实手机片段复验。

## 2026-09-07：AI 效果配方实时推导与项目上下文注入

### 本轮目标

让本地 CLI 不再从“预设效果名”查表，而是收到用户想要的效果后，实时推导出由底层能力（积木）组成的有序步骤、每个步骤的参数与时间区间；同时把编辑器当前项目状态作为上下文注入，使相对描述（如“片尾淡出”）不必再向用户追问已在环境中存在的信息。

### 范围控制

- 只实现“实时推导”这一段：参数化能力注册表、分解提示、输出协议、白名单/范围校验；不涉及渲染执行与导出。
- 上下文注入分两步落地：先注入视频事实（总时长 / 分辨率 / 播放头），再注入已执行编辑命令与字幕轨摘要。

### 实现结构

1. 积木升级为“带参数模式的能力声明”：`CAPABILITY_SCHEMAS` 六个积木（字幕、淡入、淡出 + color.grade / texture.grain / vignette），参数只支持 number，带 min/max/default/description，统一 start/end（秒）。
2. 提示词只教分解方法，不教“效果 → 固定配方”映射；无 few-shot 样例。
3. 输出协议固定为 clarify / instruction.steps，steps 数组顺序即执行顺序。
4. 校验只做白名单 + 参数模式检查：未知能力、未声明参数、越界值、非法时间一律抛 `LOCAL_CLI_INVALID_INSTRUCTION_OUTPUT`。

### 实际实现

- 参数化注册表与按 schema 的 `parseInstruction` 校验；真实本地 CLI 冒烟复现中发现 Claude Code 推导约 20s，原 15s 转换超时会被误杀，已将转换上限放宽至 60s。
- 渲染层在每次指令转换前冻结项目快照：视频事实 + 已执行编辑命令（时间轴结构化记录）+ 字幕轨摘要（按段给出序号 / 起止秒 / 文字，最多 500 段、每段 80 字），经 IPC → 主进程 → 本地 CLI 服务 → 提示词。
- 时间轴效果标记升级为保留 `capability + params` 的结构化条目，右键删除后上下文同步消失；字幕轨取已应用文字。

### 最终验证

- 全量单测：179 项中 178 通过 / 0 失败 / 1 条件跳过。
- 编辑器相关 E2E：`local-cli-effect-flow + auto-subtitles-flow` → 10/10 通过。
- 直接调用 Claude Code 的真实冒烟返回符合契约的多积木配方：color.grade / texture.grain / vignette / fade.out 组合，耗时约 20.7s（应用内已放宽超时）。

### 明确延期 / 限制

- 色偏 / 颗粒 / 暗角尚无渲染执行方：它们能推导、能通过解析，但不会落到时间轴，也不会进入“已执行编辑命令”，待渲染能力接入。
- 应用内“第二条指令自动读取第一条结果”的人工点验尚未最终确认（自动化已覆盖提示词与 IPC 透传）。
- 时间轴效果仍为会话内存状态，刷新后不恢复标记；字幕轨与对话记录已持久化。

## 2026-09-07：通用执行链周期 1 — 统一状态与字幕迁移

### 唯一目的与边界

按照已确认的周期 1 计划，建立最小统一执行主干，并让现有全片字幕完整经过它。只接入 `subtitle.generate@1`，单条 Recipe、唯一字幕轨、一次撤销和 `source.video@1 → visual.subtitle@1` 线性图；不进入周期 2，不新增 npm 依赖、数据库或 IPC，不改变颜色与布局。

### 内部完成度

- 能力注册表将声明与 prepare / toEdit / toGraph / toTimeline / preview / toExport 绑定；缺少实际 Adapter 或路由元数据的能力不对 AI 公开。提示与解析共用此目录，删除未接通效果的执行承诺。
- `ProjectEditing` 是已应用编辑的唯一写入入口。整份 Recipe 校验、准备结果、候选图编译完成后，再校验 revision 并一次保存文档与撤销记录；中途失败不改旧状态。
- `EditDocument` 持久化字幕内容，`RenderGraph` 派生预览与导出；时间轴和下一轮 AI 上下文从同一 revision 重建，并保留播放头信息。
- 旧字幕 key 保留且仅读迁移，原 `subtitle-state.js` 文件与旧格式测试保留，编辑页停止加载旧 store。未应用草稿保存到独立 sidecar。
- Tasks 3–5 的界面切换、提示词和导出接入合为一个集成提交，避免中间提交出现新旧状态接口不匹配。实现后的核查只修复既定链路问题，没有新增效果或平台能力。

### 用户可见成果

- 生成字幕后可在现有文稿区统一修改；“保存”仅存草稿，“应用”才改变视频字幕。
- 重复生成只替换一条字幕轨，整次撤销可恢复之前应用的文字与草稿。
- 刷新后恢复时间轴、字幕与可用撤销；对话历史保存失败不会把已提交编辑误报成失败，缺失的撤销入口可从项目状态恢复。
- 导出使用当前图中的已应用字幕，保留现有进度、取消、编辑器冻结、原生另存为与源文件保护。
- 未接通组件拖入只说明当前限制，不再产生假编辑记录。

### 审查发现并修正

- 反问的后续消息也必须通过草稿保存检查，保存失败不清空用户输入。
- 草稿清理失败不回滚已应用字幕；过期草稿不会在刷新后被误用并持续阻塞编辑。
- 刷新后先等待当前视频就绪，再翻译与执行；重新上传会清除旧路径。
- 导出等待项目恢复期间防止重复点击，并在等待结束后重查草稿、项目、源视频与正在执行的编辑。
- 测试使用真实临时文件路径模拟桌面导入；内存上传无本地路径，不再被测试当作可执行素材。

### 验证结果

- 基线：179 项单测，178 通过、1 条件跳过。
- 最终单元/集成：启用真实导出开关后 **197/197 通过，0 跳过**；默认测试为 196 通过、1 环境开关跳过。
- 完整 Electron 回归：**34/34 通过**，覆盖环境/CLI/工作台/侧栏/字幕/迁移/导出。
- 真实整链由代理在隔离测试项目中执行：中文语音视频 → 实际 Claude CLI → 实际 Whisper Small → 修改并应用 → 刷新 → 再生成并撤销 → 实际 FFmpeg 导出。没有替换模型或渲染输出；仅原生另存为选路由测试固定。
- 视觉检查确认编辑器与导出字幕均为“统一执行链路：字幕修改已应用”；媒体探测为 640×360、H.264/AAC、11.416667 秒，音轨非静音。测试视频与证据放在本机临时目录 `/tmp/srt-cycle1-real.JFwbe0/`，不入库。
- 没有修改用户原有项目或删除旧字幕数据；没有安装新运行时；未推送 GitHub。

### 退出与未完成项

周期 1 代码实现与代理验证完成，停止扩大本周期。用户对真实素材、原生保存对话框与外部播放器的人工点验仍待进行。完整 DAG、多能力原子事务、颜色、变换、卡片图层、关键帧、个人技能保存与复用属于后续周期，不能据本次结果声称已经支持。

## 2026-09-08：通用执行链周期 2 — 区间调色与多步骤事务

### 内部完成度

- 完整接通 `video.color.adjust@1` 的四项参数：色温、亮度、饱和度、对比度。共享参数 schema 与半开区间 `[start,end)` 规则贯通能力目录、AI 协议、持久化 edit、RenderGraph、预览和导出。
- `ProjectEditing` 对多步骤先整体校验和准备，候选图通过后一次提交、一次增加 revision；注册项决定追加或按类型替换。失败不留下部分编辑，一次撤销恢复请求前 edits。
- 视频效果按既定顺序位于字幕覆盖层之前；时间轴、预览、导出和下一轮 AI 项目上下文均读取同一文档。颜色预览仅作用于视频；导出由固定的 FFmpeg 颜色适配器与可选字幕适配器执行。
- 对话结果按事务展示、区间条显示真实起止范围；颜色与字幕组合可刷新恢复、整条撤销。调色后字幕文稿仍可继续保存与应用，恢复旧事务时同步恢复对应字幕编辑区。

### 用户可见成果

- 用户可用自然语言指定某段视频的冷暖、明暗、饱和度和对比度；多项参数形成一个“画面调色”区间项目。
- 播放或拖动到区间内立即预览，区间结束后恢复原画面；刷新后保留，可撤销并导出为真实 MP4。
- 一句话同时要求调色与字幕时，两步作为同一个请求保存和撤销。

### 自动验证

- 默认 `npm test`：232 项，230 通过、0 失败、2 个真实媒体开关跳过。
- 启用 `SRT_REAL_EXPORT=1` 的全量 `npm test`：**236/236 通过，0 失败、0 跳过**，包含已有字幕真实导出与新增四参数真实导出。
- 颜色真实导出专测：5/5 通过（1 个父测试与 4 个参数子测试），0 跳过。共用运行时生成的 4 秒多色、有音轨素材，每次只改变一个参数，在 0.5、1、2、3、3.5 秒采样；1 秒生效、3 秒已失效。
- 2 秒处相对原素材的指标：冷色蓝/红比 1.50 倍、亮度均值 1.30 倍、饱和度色度距离 1.81 倍、对比度亮度离散度 1.55 倍。所有区间外采样平均 RGB 差异低于 0.90/255（门限 4/255），输出均保留 320×240、4 秒及 H.264/AAC 音视频。
- 聚焦项目/导出/调色 Electron 回归：17/17 通过。确定性混合事务验证了一次 revision 增加、共同 transaction ID、预览边界、刷新、导出配方顺序与一次撤销；UI 成功导出夹具与真实 FFmpeg 测试分别验证，不把夹具当作真实成片。
- `test:e2e` 入口已纳入项目状态、导出和完整调色流程，保留原失败/取消场景。
- 完整 `npm run test:e2e`：**43/43 通过，0 失败、0 跳过**（串行最终复跑 2.1 分钟）；真实 Mac 只读检测另行 1/1 通过。首次全量运行因两个 Playwright 进程共用输出目录、清理了测试素材而有 1 项失败，停止并发后原断言直接通过，未为此修改产品或放宽测试。

### 真实操作验证

- 代理在隔离 userData 中启动生产入口，使用实际 Claude Code、本地视频和实际 FFmpeg；只将原生另存为的目标固定到临时目录，未替换推导或导出服务。
- 请求“5 到 10 秒明显调冷、提亮并增强对比度”，Claude 实际返回色温 -0.5、亮度 0.3、饱和度 1、对比度 1.4。应用生成一个 5–10 秒区间条，2 秒无效果、7 秒有效果、10 秒无效果；刷新保持同一文档，导出完成后一次撤销恢复原 edits，revision 单调增加。
- 成片检查为 640×360、11.416667 秒、H.264/AAC，大小 1,311,976 字节。已查看编辑器截图，成功卡片与区间条正确。
- 生产整链证据位于 `/tmp/srt-cycle2-real.IRxuRN/`（`evidence.json`、截图、`export.mp4`、隔离运行脚本）；四参数素材与成片位于 `/var/folders/dz/dz1x73p96xn6d1ss30xqk1dw0000gn/T/srt-cycle2-color-1ZajP6/`。均为本机临时验收产物，不入库。
- 以上是代理操作验证；用户真实素材的观感、原生另存为和外部播放器点验仍待用户进行。

### 退出与未完成项

本周期只交付通用颜色及其与字幕的统一事务。变换、卡片、关键帧、颗粒/暗角与个人技能按周期 3–7 开展；LUT、HDR、专业色彩管理、任意命令执行和通用 DAG 未进入本周期。预览与导出使用同一参数语义，但不承诺浏览器与编码输出逐像素相同。当前只支持撤销最近一次事务。代码保留在本地分支，尚未推送 GitHub。

### 最终审查修正：颜色预览与导出语义一致

- 修复浏览器按 R/G/B 分别调整对比度、导致对比度为 0 时错误丢失颜色的问题。预览现在由共享颜色模块生成四组冻结矩阵：温度 → RGB 转有限范围 YUV → 分别调整 Y/U/V → 转回 RGB。亮度/对比度作用于 Y，饱和度独立作用于 U/V；各阶段保留截断，多个调色节点按图顺序组合，默认参数保持原画面。固定 FFmpeg 导出、四参数协议和时间区间未改。
- 首次真实 Chromium 回归复现红色块在对比度 0 时红绿通道差为 0。首次合并矩阵修正虽通过普通参数检查，复查发现其未模拟中间平面截断：亮度 -1、对比度 2 的红块仍相差 35/255。因此该首次结果仅是部分修复，由上述四阶段实现替代。回归新增两个极端参数案例，保留中间参数、中性参数、区间、节点顺序、字幕、刷新和撤销断言，并检查两个叠加节点各自完整的四阶段顺序。
- 运行时生成三色色块视频，实际调用产品导出服务，对同一源帧的浏览器预览和 FFmpeg 成片采样比较。允许每通道最多 7/255 的 YUV/LUT 舍入与有损编码误差；四阶段实现对比度 0 的最大误差为 2/255，对比度 0.6、饱和度 0.7、亮度 0.15 的组合为 7/255，亮度 -1、对比度 2 为 2/255（红块预览 `[159,0,0]`，成片 `[160,0,0]`），对比度 0、饱和度 2 为 4/255（红块预览 `[255,57,0]`，成片 `[255,56,0]`）。未放宽 7/255 门限。该证据验证普通 SDR 素材的参数语义，不构成逐像素或专业色彩管理承诺。
- 四阶段修正后重新验证：共享颜色单测 **6/6**；真实像素对比与项目回归 **12/12**（其中像素测试 **2/2**，实际导出比较覆盖上述四组参数）；默认全量单元/集成 **231 通过、2 个真实媒体开关跳过、0 失败**；完整 Electron 回归 **44 通过、1 个真实像素开关跳过、0 失败**。新增像素回归已纳入 `test:e2e`。这些结果取代首次部分修正的验收结论。
- 本次仅处理最终审查的 Important 预览偏差。三个 Minor 加固项记录待办：数组上的特殊可执行属性、继承来的参数模式名称、损坏的持久化字幕局部区间；未在本次顺手扩展。

## 2026-09-08：通用执行链周期 3 — 区间翻转与中心缩放

### 内部完成度

- 本期功能代码完成，**桌面端最终验收尚未完成**。任务提交为 `146a258`（执行契约与真实导出）、`dd89fab`（图顺序预览与回归测试），沿用已存在的独立功能分支。
- 新增完整可执行的 `video.transform@1`：水平翻转、垂直翻转、0.25–4 倍中心等比例缩放。布尔参数保持真实类型；至少显式给出一个参数，未给出的翻转默认为 false、缩放默认为 1。同一步内先水平、再垂直翻转，最后中心缩放；范围沿用 `[start,end)`，不指定时作用全片。
- 注册项接入原有准备、edit、RenderGraph、时间轴、预览、导出六个适配器；没有另建变换专用事务、持久化或撤销流程。下一轮 AI 上下文保留 true、false、缩放值和起止区间。
- 共享整数几何控制预览与导出的尺寸舍入和居中偏移。放大裁去边缘，缩小补不透明黑边；每一步完成后保留本步裁切结果，再执行下一步，不能通过合并矩阵让已经裁掉的边缘重新出现。
- 预览以两个可复用画面缓冲区按图顺序合成变换与调色，复用原有四阶段颜色矩阵；无变换项目保留原有颜色预览。原视频仍负责解码、播放和音频，字幕与编辑器控件不随视频翻转。播放、拖动、暂停、加载、撤销及页面离开均有对应刷新或清理。
- 导出由软件生成固定的 FFmpeg split/flip/scale/pad/crop/overlay 片段，AI 不提供 Shell 或滤镜字符串。颜色与变换按图顺序处理，字幕最后叠加，输出画幅和时长不变、音轨保留。

### 用户可见成果

- “第 1–3 秒水平翻转并放大到 1.25 倍”可通过真实本地 Claude 推导为一项“画面变换”，在时间轴显示正确区间，并生成真实成片。
- 区间内预览翻转和缩放，区间外恢复原画面；刷新后编辑仍在。保留第一条编辑后新增第二条区间垂直翻转，撤销第二条只恢复到第一条编辑的状态，再刷新仍一致。
- 翻转、缩放可与已有调色、字幕共用一条事务。颜色作用于缩小前还是缩小后会产生不同且正确的黑边结果；字幕不被视频变换倒置或缩放。
- 没有改变原有界面配色、布局，也未增加旋转、位置、动画、卡片、个人技能或新运行时依赖。

### 已执行的验证

- 最终启用真实媒体的完整 Node 单元/集成测试：**260/260 通过，0 失败、0 跳过**，退出码 0。使用已安装的 FFmpeg/FFprobe；测试网络权限先单独取得，避免把本机监听权限错误当作产品失败。
- 产品导出服务的 9 个真实变换子测试通过：水平、垂直、放大、缩小、组合翻转缩放、连续缩放、混合调色/字幕顺序、奇数中间尺寸和采样宽高比。0.5、1、2、3、3.5 秒抽帧验证区间边界，输出尺寸、时长和音轨保留。
- 正式预览模块在支持的内置 Chromium 中完成 **18 项真实像素检查**，包含方向、精确起止、中心细节、黑边、连续裁切、颜色顺序和竖屏的内在尺寸。3 项 Node 生命周期测试覆盖单一刷新循环、解码帧时间、取消、重新加载、撤销及备用刷新方式。
- 对真实产品导出的 9 组视频，在浏览器中用相同源帧和正式合成器逐项对照，**580 个平坦区域采样点全部通过**。各组最大通道误差：水平 2、垂直 2、放大 2、缩小 4、组合 2、先缩小后放大 5、先放大后缩小 4、两种颜色/变换顺序均 4，单位均为 1/255；未放宽 7/255 门限。边缘重采样与字幕像素不作为源画面颜色一致性采样点。
- 对照页初次复用了编辑器控制器持有的显示画布，页面初始化清理干扰了第一组读数。将临时对照画布独立后九组全部通过；未修改产品代码或放宽断言来消除该测试页竞态。
- 两次任务审查和 `fba4cb1..dd89fab` 的全周期审查均未发现 Critical/Important 代码问题。一个 Minor 记录待办：预览合成器当前将所有异常视作源帧暂时不可用，可能掩盖意外程序错误；未发现普通有效输入的触发路径，本期不追加错误监测系统。

### 真实操作证据与验收限制

- 使用隔离的临时项目、实际 Claude Code、正式编辑器脚本和实际产品 FFmpeg 导出服务；**仅把 Electron 传输层替换为本机回环测试接口**。这证明 AI、项目状态、浏览器预览和产品导出协同工作，但不是生产 Electron IPC 的端到端证明。
- 第一条真实请求返回 1–3 秒水平翻转、1.25 倍缩放；成片 `output-1.mp4` 为 640×360、4.000 秒、H.264/AAC。真实编辑器在 2 秒处与成片的五处采样最大差为 1/255，包含能识别中心放大的偏心白色细节。
- 第二条真实请求读取 revision 1、已有翻转 true/false、1.25 倍缩放和 1–3 秒区间，只新增 3–4 秒垂直翻转。时间轴保留两条；一次撤销恢复仅有第一条，刷新后仍保留恢复结果。
- 浏览器整链证据：`/tmp/srt-cycle3-browser.WMherA/` 中的 `evidence.json`、`preview-export-parity.json`、`preview-at-2s.png`、`source.mp4` 和 `output-1.mp4`。最终真实变换素材位于 `/var/folders/dz/dz1x73p96xn6d1ss30xqk1dw0000gn/T/srt-cycle3-transform-hpO4l7/`。均为本机临时验收产物，不入库；用户原有项目未被修改。
- **未通过/未执行的桌面验收不得记为通过：** 本环境启动 Electron 时在 macOS `_RegisterApplication` 阶段 SIGABRT，尚未进入应用 JavaScript。所需文件和网络权限未解决该启动问题，没有绕过沙箱。6 个新增 Electron 测试已收集，但正式 Electron UI/IPC、完整桌面回归及竖屏截图测试仍待可启动 Electron 的环境补跑；本期没有重跑大量必然无法启动的窗口。用户真实素材及原生保存对话框人工点验也仍待进行。
- 额外观察到：真实 CLI 对“只说明当前已应用的编辑，不要新增或改变编辑”的纯询问信息被解析为无效编辑输出，项目未被改动。该问题单列为后续对话协议工作，不因本轮变换成功而宣称自然对话已经完全正常，也不在本期顺手重写。

### 退出状态

本期代码实现、后台真实媒体验证和支持的浏览器验证完成，**保留桌面完整验收缺口，不将第三周期标记为全部验收结束**。下一步仅补桌面启动环境与人工验收；不进入第四周期、不自动推送 GitHub。常见 SDR 素材的预览/导出语义经过验证，不承诺编码前后逐像素相同，也不扩展专业色彩、极端媒体格式或平台级故障恢复。

## 2026-09-08：第三周期桌面验收补测

### 内部完成度

- 经正常权限审核后 Electron 可以启动，未改产品启动代码。此前“无法启动”的环境限制已解除。
- 本次首次完整桌面回归为 47 通过、4 失败。失败都来自新增变换测试的三类断言问题：纯变换错误查找字幕状态；通用提交函数误要求反问和失败也折叠；截图颜色经过显示转换后与原始 Canvas RGB 不完全相同。
- 仅修改 `tests/e2e/transform-edit-flow.spec.js`：分别校验指令/执行结果与折叠行为；按字幕/普通编辑选择状态；增加原始 Canvas 精确 RGBA 断言，再用同一截图内未经变换的源 Canvas 校准色精确检查翻转后的颜色。黑边、位置、透明度仍严格验证，没有提高容差或改产品效果。独立只读审查无阻塞发现。
- 专项 6/6 通过；最后完整 Electron 回归 **51/51 通过，0 失败、0 跳过**，启用真实调色导出像素对比，耗时 2.5 分钟，退出码 0。
- 同版本 Node/真实媒体验证重新运行：**260/260 通过，0 失败、0 跳过**，退出码 0。

### 用户可见成果

- 桌面流程实际验证了字幕修改/草稿、调色、区间变换、混合事务、刷新恢复、上下文、撤销及导出状态；模拟 CLI/字幕服务的测试不被当作真实 AI 证据。
- 正式 Electron 主程序另用隔离项目导入真实色块视频，直接提交固定测试配方（不调用 AI），实际解码采样 0.5、1、2、3、3.5 秒，确认只在 `[1,3)` 水平翻转并中心放大；刷新保留文档与渲染图，撤销后再刷新仍一致。
- 从正式桌面导出按钮经默认 IPC 和真实 FFmpeg 产生 `local-export.mp4`：640×360、4.000 秒，保留音轨。仅自动指定临时保存路径，没有替换渲染服务；这证明正式桌面执行/导出链路，不证明本轮真实 AI 请求或原生保存对话框操作。
- 实际编辑器竖屏截图已保存并查看，显示居中缩小、四周黑边以及正确翻转；左上角的小色块仅为测试截图校准，不是产品新增界面。

### 仍需完成与证据

- **真实 Claude → 正式桌面 IPC 的对话链路仍待批准。** 自动权限审核拒绝本轮向 Claude 发送测试指令及项目摘要，已询问用户是否允许发送人工生成素材的测试信息；没有借其他调用方式绕过拒绝，也没有把此前回环浏览器证据算作本次正式 IPC 通过。
- **原生保存/取消按钮人工操作仍待解锁。** 桌面操作工具报告 Mac 锁屏，需用户手动解锁；程序化指定保存路径和已有模拟取消测试不替代这项。临时验收 bootstrap 的 Electron 模块导入错误已更正，正式应用代码未变。
- 完整桌面测试证据：`/tmp/srt-cycle3-desktop-final-20260908/`，含竖屏 `portrait-in-editor.png`。本地正式 IPC 证据与成片：`/tmp/srt-cycle3-native.PB3D1k/local-evidence.json`、`local-export.mp4`、`local-preview.png`。本次真实变换测试素材：`/var/folders/dz/dz1x73p96xn6d1ss30xqk1dw0000gn/T/srt-cycle3-transform-msyAMa/`。
- 隔离测试窗口已关闭，用户原项目没有改动。本次只有验收测试与记录修改，尚未提交/推送；不进入第四周期。上述两项和用户自己的素材观感确认完成前，第三周期仍不标记为全部验收结束。

## 2026-09-08：获准后的真实 AI 与原生保存点验

用户明确允许向已登录 Claude 发送测试指令及人工素材项目摘要，不上传视频；随后执行正式 Electron 的真实 AI 流程。没有替换 AI 或渲染服务，首条整链仅注入临时保存目标，并旁路记录请求/结果作为证据。

### 已验证成果

- 实际 Claude 在约 9 秒返回“1–3 秒水平翻转并中心放大 1.25 倍”，只提交一条编辑、一次 revision。0.5、1、2、3、3.5 秒真实解码采样验证区间，刷新后文档/渲染图保留。
- 从正式 UI 经 IPC/FFmpeg 导出 `export.mp4`：640×360、4.000 秒、H.264/AAC。2 秒处五点预览/成片最大通道差 1/255，未放宽 7/255 门限。
- 原生保存对话框第一轮取消后显示“已取消”，能再次导出；随后确实保存出默认文件名的成片。第一次临时验收脚本误要求 `dialog-export.mp4`，因实际使用默认文件名而中止，不能记为整段 PASS。第二次点验期间窗口到达“导出完成”，而脚本仍在等待取消，已停止自动化，未继续争抢窗口操作。
- 独立核对第二次真实保存产物 `/tmp/srt-cycle3-dialog.H02Wyk/dialog-source-已编辑.mp4`：640×360、4 秒、H.264/AAC。原生取消和保存有实际观察/产物证据，但临时脚本顺序未完整跑通；与上一轮模拟保存路径明确区分。

### 新发现的真实缺陷：连续编辑重复追加旧操作

- 第二条输入：“保留刚才已经执行的编辑。在第 3 秒到第 4 秒新增垂直翻转，缩放保持 1 倍。”
- IPC 证据确认发送了 revision 1、已执行的水平翻转/1.25 缩放、原时间区间及播放头位置。**上下文已到达，CLI 连接正常。**
- Claude 在约 11.4 秒返回“旧操作 + 新操作”两步；执行器按追加语义处理，导致项目总编辑数从 1 变成 3，而不是预期 2。真实测试在此断言失败，之后的撤销/再次刷新断言未运行，不能用已有模拟测试代替这段真实验收。
- 原因定位：`src/instruction-capabilities.js` 的 `buildPrompt` 未声明 `steps` 仅代表本次新增操作；`src/project-editing.js` 则保留旧文档并逐项追加。独立只读审查认定为 Important 的提示词/执行契约缺口，而不是测试误报或传输故障。
- 建议修复边界：明确“已应用内容是现状，保留它们不需要重发；本次只输出新动作”，同时保留用户明确要求重复叠加的合法语义，增加相应回归并复跑原两条输入。不做按参数强制去重，不在验收请求下擅自扩展新功能或重构执行器。

证据保留于 `/tmp/srt-cycle3-native.PB3D1k/` 的 `ipc-evidence.json`、`evidence.json`、`failure.png`、`preview-at-2s.png` 和首条成功的 `export.mp4`。本轮未改产品代码、未提交或推送。**第三周期仍不能标记为全部验收通过：真实连续对话缺陷待用户确认修复；用户个人素材的体验验收也仍独立保留。**

## 2026-09-08：连续编辑增量协议修复与真实复测

用户批准“继续修复”后，仅修正上述提示词/执行契约缺口；本节取代前节“缺陷待修复”的当前状态，原失败证据保留。

### 内部完成度

- 产品仅修改 `src/instruction-capabilities.js` 的 `buildPrompt`：明确 `instruction.steps` 是本次新动作，而不是整个项目配方。已应用上下文仍完整注入；保留或继续现状不代表重做旧操作。
- 明确区分调色/变换的追加叠加与重新生成字幕的替换行为。用户明确要求再次执行时，即使参数完全相同，也允许产生新动作；没有按参数强制去重、重构执行器或添加效果映射表。
- 仅要求保持现状时，沿用 `clarify` 说明无需变更，不编造步骤。此项不代表此前所有纯信息询问均已修复。
- 先运行新增回归，44 项中 41 通过、3 个新增提示词约束失败；补充协议后，提示词/项目执行/CLI 专项 **63/63 通过**。新增项目测试确认两个独立的相同变换请求仍各自追加，撤销只移除最近请求。
- 启用实际 FFmpeg 的完整 Node/媒体测试 **264/264 通过，0 失败、0 跳过**，退出码 0。独立只读审查覆盖本轮三个产品/测试文件，无 Critical、Important 或 Minor 发现。
- 修复后重新运行完整 Electron 桌面回归，**51/51 通过，0 失败、0 跳过**，耗时 2.5 分钟，退出码 0，包含真实调色预览/导出对照。输出目录 `/tmp/srt-incremental-fix.vrp6cz/e2e/`；该套测试的模拟 AI 场景与下述真实 Claude 调用分开计证。

### 用户可见成果与真实调用

- 使用隔离项目和正式 Electron 主程序、生产 IPC、真实 Claude 与实际 FFmpeg；仅固定临时导出目标，并记录请求结果。按已获授权只发送测试文字与人工素材的项目摘要，不上传视频。
- 原先失败的两条输入保持不变：先“在第 1 秒到第 3 秒之间水平翻转画面，并以中心为锚点放大到 1.25 倍，其他时段保持原样。”，再“保留刚才已经执行的编辑。在第 3 秒到第 4 秒新增垂直翻转，缩放保持 1 倍。”。第二次真实结果只含新动作，总编辑数为 2，第一条内容未变；第二次请求确实带有 revision 1 和第一条编辑。撤销第二条、刷新后仍只保留第一条。
- 追加实际请求“再次执行一次水平翻转并中心放大到 1.25 倍……明确要再叠加一次相同操作”，确认相同参数仍产生独立编辑。再请求“保持现状，不要执行任何新的编辑。”，得到 `clarify`，文档、渲染图和 revision 均未改变。四次真实调用全部通过，页面无运行错误。
- 第一条请求的预览在 0.5、1、2、3、3.5 秒验证区间与中心缩放，刷新保留；从正式界面实际导出 `export.mp4`，640×360、4.000 秒、H.264/AAC。2 秒处五点预览/成片最大通道差 1/255，未放宽 7/255 门限。
- 新证据位于 `/tmp/srt-incremental-fix.vrp6cz/`：`evidence.json`、`ipc-evidence.json`、`preview-at-2s.png`、`source.mp4`、`export.mp4`。原失败目录未覆盖；所有媒体和测试项目均为临时验收数据，不入库、不改用户原项目。

### 验收边界

本次真实复测通过，说明原先发现的重复执行问题在这些输入下已修复；提示词约束不等同于对模型所有未来输出的确定性保证。用户个人素材的观感验收仍独立待做，先前原生对话框点验的脚本限制仍按原记录保留。不增加新能力、不进入第四周期。用户随后批准将本轮修复、验收测试和研发记录与第三周期已有提交一起推送到 `feature/subtitle-export-cycle0`，不合并主分支；该发布授权不等于个人素材验收通过。

## 2026-09-08：第四周期静态文字与矩形图层

用户在第四周期目标、时间与边界报告后批准开发。本期从 `9abf439` 开始，沿用现有工作树和执行链；约 06:22 UTC 开始，约 07:06 UTC 首个桌面流程可检验，未扩展到下一周期。

### 内部完成度

- 新增两个独立积木：`visual.shape@1`（纯色矩形）和 `visual.text@1`（纯文本）。能力声明、准备、编辑文档、渲染图、时间轴、预览和导出接入既有注册表及 ProjectEditing；不是一个专用“卡片效果”执行器，也没有第二份持久化图层状态。
- 共享归一化坐标、像素取整、字体与基线计算。源视频调色/变换在下，文字与矩形按编辑顺序覆盖，字幕始终在上。文字固定 Heiti SC，最多 200 个 UTF-16 单元、8 行，只处理显式换行，不自动换行或缩字；颜色为纯色 `#RRGGBB`。
- 导出继续使用实际 FFmpeg：矩形由固定 drawbox 片段生成，文字写入任务目录内固定命名的 UTF-8 文件，drawtext 禁用表达式展开。AI 不提供文件路径、滤镜字符串、Shell 或可执行代码。
- 分任务先写失败测试，再实现、审查。审查发现并修复：极小矩形取整为零导致 FFmpeg 意外大块覆盖；未知 `constructor` 参数绕过共享白名单；进度事件覆盖已解码帧时间；拖动期间刚清空的图层又被事件画回。正尺寸/字号/行距统一至少 1 像素，播放刷新保留解码时间，拖动期间禁止绘制和排队。没有因此建设新调度平台或状态监控系统。

### 用户可见成果

- 文字和矩形可组合成多个静态提示卡，支持各自出现区间、位置、颜色、字号和前后遮挡。一次请求的四个元素在时间轴和结果摘要中只显示一条“静态图层 · 4 个元素”，内部仍保存四项编辑。
- 图层随项目持久保存，刷新恢复，整次请求可以撤销；下一条指令的上下文包含已有图层的文字和参数。已有字幕草稿、调色和翻转/缩放流程保持原有行为。
- 没有改动原有界面配色、总体布局或新增属性面板。本期只支持静态矩形与纯文本的新增/组合及整次撤销，不包含动画、图片、富文本、模板、个人技能、图层属性修改或排序界面。

### 最终本地验证

- 主代理在最终代码 `edc5749` 独立复跑：启用真实 FFmpeg 的完整 Node/媒体测试 **282/282 通过，0 失败、0 跳过**，退出码 0；完整 Electron 桌面回归 **54/54 通过，0 失败、0 跳过**，耗时 2.7 分钟，退出码 0。
- `9abf439..edc5749` 全周期独立只读审查通过，无 Critical、Important 或 Minor 待修项。两次任务审查提出的问题及专项 RED/GREEN 证据保留在任务报告中。
- 使用隔离的人工色块视频，**仅将 AI 翻译替换为固定本地测试数据、保存目标替换为临时路径**；正式编辑器、生产 IPC、项目模块、预览和 FFmpeg 导出均正常执行。不调用外部 AI、不修改用户原项目。
- 一次请求保存两张重叠卡片（四个元素、一个事务、一条时间轴记录）。通过正常 seek/play/pause/ended 事件，在 0.5、1、1.5、2、2.5、3、3.5、4 秒检查显示、遮挡和消失；没有手动调用预览刷新来补齐链路。文字预测区域实际有 264 个明亮像素，结束时图层确实清空。
- 正式导出按钮生成真实 `export.mp4`，640×360、4.000 秒，保留音轨。2.5 秒处 60 个实心重叠区域的预览/成片采样最大通道误差为 **1/255**，未放宽 7/255 门限。已查看实际编辑器截图和成片抽帧，中文字形、位置与重叠顺序正确。
- 固定第二条测试配方收到当前 revision 和已有文字/矩形参数，只追加一项红色文字；撤销第二条、刷新后保留原先四项。该结果证明本地上下文传递与增量执行，**不证明真实模型本轮会如何推导**。
- 同一固定字体的独立 Canvas/FFmpeg 几何探针，中文与拉丁文字边界差异不超过 1 像素；实际 Electron 测试补实了竖屏、裁切及字幕与图层重叠的像素断言。不承诺两种渲染器字形抗锯齿逐像素相同或跨平台字体完全一致。

证据：`/tmp/srt-cycle4-local.P4jiK1/` 中的 `evidence.json`、`local-calls.json`、`editor.png`、`preview.png`、`export-at-2.5s.png` 和 `export.mp4`；完整最终桌面结果位于 `/tmp/srt-cycle4-final.WLE5yt/e2e-patched/`；字体探针位于 `/tmp/srt-cycle4-font.BJ44WT/`。均为临时验收资料，不入库。修正前的一次测试仅报告继承环境的 NO_COLOR/FORCE_COLOR 日志着色冲突；最终运行去除该冲突环境变量，没有为此修改产品。

### 尚未完成的验收与退出状态

- **真实 Claude → 正式桌面 → 导出的整链验收尚未执行。** 本轮尝试在进程启动前被自动安全审核拒绝，理由是对外发送项目摘要需要明确授权。已请求允许向已登录 Claude 发送两次测试编辑要求、能力说明和人工视频/图层摘要，不上传视频、不发送路径或真实项目数据；当前尚待回复。没有换路重试或将固定配方冒充真实 AI 验收。
- 用户个人素材的观感验收仍待进行；本轮程序化指定保存路径，不宣称重新完成原生保存对话框人工点验。
- **开发和本地验收已完成，保留真实 AI 验收缺口，不标记为全部验收结束。** 本次只做本地提交并保留分支/工作树，不自动推送 GitHub，也不进入第五周期。

### 获准后的真实 AI 验收

用户随后明确回复“允许”。按授权仅发送两次人工测试指令、能力说明和人工素材的项目摘要，没有上传视频或真实项目数据；此前被拒绝的尝试没有产生外部调用。本节取代上一节“真实 AI 尚待批准”的当前状态，保留原记录。

- 使用正式 Electron 主程序、生产 IPC、已登录 Claude 和真实 FFmpeg，在隔离项目中执行；没有替换翻译或渲染服务，只自动指定临时导出目标。产品代码仍为已完整测试的 `edc5749`，本轮不改功能。
- 第一条要求在 1–3 秒显示黑底白字“重点”，2–4 秒显示向右下偏移的蓝底白字“补充”，并让第二张遮住第一张的重叠部分。真实调用耗时 **28.872 秒**，返回“矩形 → 文字 → 矩形 → 文字”四步及各自的位置、字号、颜色和区间；项目实际保存四项编辑、一个事务、一次 revision，时间轴显示一条组合记录。
- 正常拖动和播放验证单卡、重叠双卡、单卡及片尾消失，未手动触发预览刷新；重新加载保留完整编辑文档和渲染图。从正式导出按钮实际生成 **640×360、4.000 秒且带音轨的 MP4**。2.5 秒处 21 个实心图层区域采样，预览与成片最大通道差 **2/255**，7/255 门限未改。
- 第二条要求保留卡片，仅在 0–1 秒的画面下方新增红色纯文字“开始”。真实调用耗时 **11.022 秒**；请求上下文包含 revision 1、原四项图层的文字、颜色、位置、顺序和区间。模型只返回一项新增文字，原四项未变；撤销第二次请求并重新加载，仍恢复为原四项。
- 验收脚本退出码 0、结果 PASS，无页面运行错误。独立只读复核确认恰好两次翻译调用和一次导出，实际响应、项目保存、下一条上下文、撤销及导出结果相互对应；没有追加第三次外部调用。

已查看实际编辑器截图及导出抽帧：后画的蓝色卡片确实覆盖先画的黑色卡片，包括部分“重点”文字。这符合本次明确要求的重叠遮挡，但不代表自动避让文字或智能排版已实现。纯色采样不证明文字抗锯齿逐像素一致；文字几何与整组撤销另有此前本地/桌面测试，本次真实对话直接验证的是第二条新增文字的撤销，没有在第二条后再次导出。

证据位于 `/tmp/srt-cycle4-real.qzeGOu/`：`evidence.json`、`ipc-evidence.json`、`editor.png`、`preview.png`、`export-at-2.5s.png`、`export.mp4`。临时素材和隔离项目不入库，不改用户原项目。本轮只补真实 AI 验收，没有重新运行此前已通过的 282/282 Node/媒体和 54/54 Electron 全套测试。

**第四周期开发、自动化回归及本次真实 AI 整链验收已完成。** 用户个人素材的观感确认仍单独待做；本次不宣称重新完成原生保存对话框人工点验，不保证所有未来模型请求均成功。只更新本地验收记录，不推送、不进入第五周期。

## 2026-09-08：第五周期组合透明度与缩放动画

用户明确允许将第四周期最新完整版本备份至 GitHub 后继续开发。开工前已正常推送 `b530e2da1fc083bcfa0a41b85c6693bb120bf4c0` 至 `trydays/srt` 的 `feature/subtitle-export-cycle0`，并用远端引用独立核对。第五周期继续使用原隔离工作树，不合并主分支、不另建执行平台。

### 内部完成度与边界

- 功能代码及本地验收完成，真实 Claude 验收尚待授权；不能称为全部验收结束。约 07:56 UTC 开始，08:58 UTC 首个正常桌面动画场景通过，09:15 UTC 最终独立本地整链通过，在三小时可见检查和六小时止损线内。
- 新增平面组合积木 `visual.group@1`，顺序容纳 1–16 个已有矩形/纯文本元素。沿用原来的准备、编辑文档、渲染图、时间轴、预览和导出适配器及统一事务；不做“弹出卡片”专用预设，不另存动画状态。
- 透明度 0–1、等比例缩放 0–2，可为常量或 2–16 个关键帧。时间相对组合起点，首帧为 0，严格递增且不超出组合区间；目标帧指定线性、缓出或固定轻微回弹，末帧保持至区间结束。共享计算与受控 FFmpeg 表达式保持同一语义。
- 文字/底板先合成为视频内在画幅大小的透明面，再围绕固定中心点整体变换和施加一次透明度，避免交叠处重复变暗。画幅外内容先裁切，不能因缩小从画幅外重新进入。预览/导出共用整数几何和半开区间，源画面效果在下、字幕在上。
- 导出复用现有 FFmpeg 任务、取消、音轨映射、源文件保护与最终文件检查；用户文本进入固定命名的文字文件，禁用表达式展开。模型不提供 Shell、路径或滤镜表达式。
- 默认 AI 目录在真实导出和桌面接通后才开放组合能力。提示词输出完整嵌套参数模式，后续上下文保留已应用的元素及关键帧；只输出本次新增动作。不能原地修改已有图层时明确反问，不假装替换。
- 仅新增平面组合的透明度/缩放动画；不包含位移、旋转、自定义曲线、嵌套组、旧图层引用改造、图片、个人技能库或新属性面板。源视频的已有缩放仍为静态区间操作，UI 配色/布局未改。

### 用户可见成果

- 文字和底板能够作为一个整体淡入、放大回弹、保持和淡出；正常播放、暂停、前后拖动均按媒体时间显示，项目重开后恢复相同动画。
- 每次请求仍只显示一条简洁记录；带组合的记录显示“动画图层”，元素数量按实际子元素累计，旧静态请求仍显示原标签。
- 动画配方应用后可保存、撤销最近一次请求并导出真实成片；下一条新增文字等请求不会因保留旧动画而重新执行旧配方。本周期不增加多级撤销。

### 分任务验证与修正

- 任务 1：共享关键帧、平面组和项目/配方契约；先 RED 后实现。审查发现独立关键帧 API 用 map 跳过稀疏数组空位，导致返回不可计算的数据；用直接回归复现后增加三行逐索引检查，专项 12/12。未扩大成通用校验框架。
- 任务 2：实际导出专项 26/26，完整实际媒体 Node 309/309；真实四秒素材验证回弹、半透明交叠、两行中文、区间边界、媒体信息和源保护。旧 sandbox 回环 EPERM 经正常权限审核后解决，没有改产品去规避权限。
- 任务 3：相关 Node 51/51，最终 Node 314/314、Electron 57/57。桌面夹具修正了超出已有矩形限制的参数、字幕状态选择器及错误的多级撤销假设；一个旧提示词格式断言更新为完整 JSON 模式。没有为这些夹具问题改变产品行为。
- 三个任务均经独立只读审查；最终 `b530e2d..443f523` 全周期代码审查通过，无 Critical、Important 或 Minor 待修项。代码提交为 `2378ef2`、`cfdcb5c`、`3409c05`、`443f523`，其余本轮提交为计划/报告。

### 主代理最终独立验收

- 在最终产品代码 `443f523` 独立重跑真实 FFmpeg Node/媒体：**314/314，0 失败、0 跳过**，退出码 0；完整 Electron：**57/57，0 失败、0 跳过**，2.8 分钟，退出码 0，含原有实际调色预览/导出对照。桌面产物 `/tmp/srt-cycle5-final.6nyXph/e2e/`。
- 正式 Electron 主程序、生产 IPC、生产配方解析、真实 FFmpeg 和隔离项目运行；**翻译替换为两条固定本地测试配方，另存为目标自动指定临时路径**。未调用外部 AI、不改用户真实项目。这是本地整链证据，不是真实模型推导证据。
- 第一条配方为 1–3 秒的黑底白字两行组合，前 0.5 秒淡入并从半尺寸放大回弹，末 0.5 秒淡出。实际提交一项组合、一次 revision、一条时间轴记录；正常 seek/play/pause/ended、逆向重复采样、刷新恢复通过，没有手动触发绘制来补齐播放。
- 正式导出按钮生成 **640×360、4.000 秒、H.264/AAC MP4**。六个动画时刻累计 **1013 个实心区域采样**，最大通道差 **7/255**；几何在原定 1 像素容差内，0.5/1/3/3.5 秒检查起止及区间外源画面。源视频左上角逐帧变色标记独立验证取的是对应帧。字形已查看，不承诺抗锯齿逐像素一致。
- 第二条固定配方只新增 0–0.5 秒的红色文字；IPC 记录带 revision 1 和完整组合/关键帧参数，原组合未变，撤销第二条并重开后仍保留原组合。恰好两次固定翻译调用及一次真实导出，脚本 PASS/退出码 0，无页面错误。

首次临时整链在 `31/24` 秒的源帧标记处失败：浏览器把 `1.291666666...` 截为 `1.291666`，实际显示前一帧。独立、不加载任何产品代码的最小视频页面两次复现，等三次绘制帧也不改变；略进入下一微秒则得到预期帧。这排除了产品绘制和读取过早的假设。仅在临时脚本中对不可微秒表示的帧时刻向上对齐，并记录实际跳转时间；FFmpeg 仍按准确帧索引抽取，像素/几何门限未变，产品代码未改。原失败 `/tmp/srt-cycle5-local.DwRXjn/` 与最小探针 `/tmp/srt-cycle5-seek-probe.qKpJ2i/` 保留。

最终本地证据及成片位于 `/tmp/srt-cycle5-local-final.hri4Fa/`：`evidence.json`、`ipc-evidence.json`、`editor.png`、`preview.png`、`export-at-1.5s.png`、`export.mp4`。均为人工素材和临时隔离项目，不入库。

### 待完成与收尾

- **本轮没有执行真实 Claude 调用。** 已非阻塞请求允许发送两条人工测试要求、能力说明及人工项目摘要（不上传视频、不发真实项目数据），当前尚未收到这一项的回复；没有用固定配方冒充真实 AI 验收或复用上一周期的两次调用授权。
- 用户自己的素材观感与原生保存对话框点验仍独立待做；本轮不宣称完成这些人工体验验收，也不宣称 4K 多组性能或任意自定义算法已可用。
- **第五周期开发及本地验收交付，保留真实 AI 验收门槛。** 仅在本地保存新提交和更新研发日志/项目状态，保留现有分支和工作树；不再次自动推送、不合并主分支、不进入第六周期。

### 获准后的第五周期真实 AI 验收

用户随后明确回复“允许。”，授权两次人工指令和人工项目摘要的真实 Claude 验收。本节更新上述待授权状态，保留之前的本地验收记录。本次仅补验收，不修改产品代码；产品仍为此前完整回归与审查过的 `443f523`。

- 原先准备好的 `/tmp/srt-cycle5-real.qMHHAs/run.cjs` 只执行一次。使用正式 Electron 主程序、生产 IPC、默认 Claude 翻译与 FFmpeg 服务，旁路记录请求/结果，只自动指定临时导出目标；没有替换模型返回值。隔离人工四秒素材与项目，不上传视频，不将素材路径或真实用户项目数据交给翻译。
- 第一条要求 1–3 秒在画面中央显示黑底白字两行“重点／补充说明”，前 0.5 秒整体淡入并从 50% 放大、轻微回弹，最后 0.5 秒整体淡出。真实调用 **50.186 秒**，返回一个 `visual.group@1`，包含一个矩形和一个显式换行文本元素、四个透明度关键帧、两个缩放关键帧及 `back-out` 缓动。实际保存一项组合、一次 revision、一条时间轴记录，未使用卡片效果查表或固定测试响应。
- 13 个正常跳转时刻验证透明度、尺寸、固定中心和半开区间；独立计算的回弹峰值约 **1.050 倍**，发生在 1.291667 秒附近。逆向跳转重复像素、正常播放/暂停/片尾清空、完整文档与渲染图重开恢复均通过，没有手动调用绘制函数补齐链路。
- 从正式导出按钮生成 **640×360、4.000 秒、H.264/AAC 且有音轨的 MP4**。10 个实际成片帧检查源帧身份、区间外恢复、动画边界；六个有效动画帧累计 **765 个实心区域采样**，最大通道差 **6/255**，几何在 1 像素内。沿用已验证的临时脚本微秒帧对齐，未放宽 7/255 或 1 像素容差。
- 第二条要求保留原动画，仅在 0–0.5 秒的下方新增红色纯文字“开始”。真实调用 **10.288 秒**，上下文带 revision 1、已有组合全部子元素与关键帧；只返回一项新增文字，原组合完全不变。撤销第二条并重新加载，原组合及对应状态仍保留。
- 脚本 **PASS、退出码 0、无页面错误**；记录恰好两次翻译和一次实际导出，没有第三次模型调用。实际编辑器预览及成片抽帧已查看，黑底白字两行可见；左上角小色块是人工素材的逐帧校验标记，预览中央播放按钮不进入成片。
- 独立只读证据审计通过，无阻塞问题；核对真实默认服务、两次调用计数、响应与项目/导出对应关系，并确认翻译请求仅含人工文字、历史和项目摘要，素材路径只出现在本地导出请求中。本次运行开发态 Electron，不代表已验收打包后的发行版。

证据与成片：`/tmp/srt-cycle5-real.qMHHAs/` 中的 `evidence.json`、`ipc-evidence.json`、`editor.png`、`preview.png`、`export-at-1.5s.png`、`export.mp4`。临时素材与隔离项目不入库。本轮没有重跑此前已通过的 **314/314 Node/媒体** 和 **57/57 Electron** 全套回归，也没有修改测试或功能代码。

**内部完成度：第五周期实现及计划内自动化/真实 AI 整链验收完成。用户可见成果：AI 实际推导的组合淡入、缩放回弹、保持、淡出已进入真实 MP4；下一条新增指令与撤销/重开正确。** 本次不证明任意需求均能成功；首条约 50 秒，实际生成仍有等待成本。整组首条撤销另有此前确定性桌面测试，本次真实调用直接验证第二条新增的撤销，第二条后未再次导出。用户个人素材观感和原生保存对话框人工点验仍单独待做，不承诺字形抗锯齿逐像素相同、4K 多组性能或新算法支持。只更新本地记录，保留分支和工作树，不推送、不进入第六周期。

## 2026-09-08：第六周期区间颗粒与暗角

用户在第五周期最新版本已推送后明确要求继续下一阶段。开工基准为已推送并核对的 `bdfc4225b4935284cb0ecdac6b281b0b97ff98bc`，沿用现有隔离工作树与 `feature/subtitle-export-cycle0` 分支。本轮约 09:50 UTC 开始，10:29 UTC 出现可检验桌面效果，约 11:08 UTC 完成本地整链及阅片，在两小时可见检查、四小时止损线内；不自动推送或进入个人技能库周期。

### 内部完成度与能力边界

- 开发和本地验收完成，真实 Claude 验收仍待本轮授权。新增 `video.noise@1` 与 `video.vignette@1`，各有完整的校验、准备、编辑、图、预览和导出适配器，复用统一事务/持久化/撤销/上下文，默认 AI 目录由六项扩展到八项。
- 单色细颗粒强度 `amount` 0–1，按媒体时间固定每秒 12 个纹理时段；固定确定性散列与微秒时间约定，使相同源像素/时间/参数得到相同结果。中心圆形暗角强度 `strength` 0–1，按原视频内在像素计算距离。两者均支持半开时间区间，零强度为恒等、alpha 不变。
- 共享小模块 `src/video-texture.js` 同时提供受限参数、CPU 像素算法和软件生成的 FFmpeg 表达式；浏览器复用两张已有缓冲画布，不增加渲染循环、帧缓存或新运行时依赖。私有 65,521 字节定值查找表仅加速固定散列，不记录随机/项目状态。
- 颗粒、暗角与已有调色/变换保持源效果顺序，文字、矩形、组合动画在上，字幕最后叠加。模型只收到参数模式与分解方法，不加入“复古→固定脚本”的预设映射；旧的不可执行 `texture.grain@1`、`vignette@1` 继续拒绝。
- 不做颗粒大小/种子/频率编辑、移动暗角、模糊、LUT、Shader、任意代码或算法安装、效果库、新面板、个人技能保存。UI 配色与布局不变，不承诺高分辨率多效果实时性能。

### 用户可见成果

- 颗粒和暗角可独立使用，也可与已有画面调整共同应用、正常播放和前后拖动；时间轴显示实际效果，聊天仍把整次请求合为一条简洁记录。
- 已应用参数、时间区间和顺序进入下一条指令的项目上下文；新增文字不重复执行旧效果，撤销最新请求后旧效果保留，重开后状态一致。
- 真实 MP4 保留画面效果、原时长/画幅和音轨；不只是生成指令或添加时间轴标记。

### 验证与有限修复

- 基线真实 FFmpeg Node/媒体 **314/314**。任务一先写缺失模块/适配器的 RED，再实现并完成独立 raw RGB 算术对照、横竖屏、零/最大强度、起止边界、两种效果顺序，以及真实颗粒、暗角和调色/变换/字幕组合导出。独立复审指出首版把产品像素函数当作 oracle、媒体矩阵不足，已用测试内独立公式和实际成片断言补齐；未借此扩充产品能力。
- raw RGB 误差门限为 1/255；纯灰颗粒 MP4 的强度保留比 0.80–1.15、均值偏移不超过 3、相关性至少 0.90。噪声不套用平滑色块的逐像素 7/255 门限；平滑暗角和区间外色块仍用原门限。早期测试中的 alpha 平面假设、彩条角点编码振铃通过纠正测试素材/读取方式处理，没有放宽冻结门限。
- 任务二接入实际桌面播放及默认 AI 目录。独立复审发现旧跳转被换素材中断后 `seeking` 状态不清零，导致预览一直受抑制；新增回归 **4 通过/1 失败**，最小修复后 **5/5**。修复只分离源重置事件与跳转事件，没有重构其它渲染器。
- 补齐实际播放颗粒变化、片尾稳定、双尺寸/双顺序独立 RGB、紧凑历史与时间轴、后续指令/撤销/重开、字幕草稿，以及实际页面不透明卡片在源颗粒之上的检查。最后一项使用 source+layer、layer-only、video-only 三张页面 PNG 作零容差对照：卡片内前两张相同，图层基准区别于原视频，卡片外源效果实际产生变化。没有硬编码观察到的截图颜色，也没有更改产品色彩处理。
- 分任务复审和全周期 `bdfc422..53dee81` 独立只读审查通过，无 Critical、Important 或 Minor 待修项。产品最终为 `0b44335`，之后 `53dee81` 仅增强测试。

### 主代理最终独立验收

- 在 `53dee81` 完整复跑：真实 FFmpeg Node/媒体 **335/335**、完整 Electron **63/63**，均退出码 0、零失败、零跳过。最终桌面产物 `/tmp/srt-cycle6-final.zHLxVC/e2e-final/`。修复前中间回归与最终结果分别记录，不重复冒称最终验收。
- 正式 Electron 主程序、生产 IPC、生产配方解析、项目状态、预览与真实 FFmpeg；仅将翻译替换为两条明确的固定测试配方，并自动指定临时另存为路径。使用人工四秒视频、隔离项目，不修改用户真实素材或项目，不调用外部 AI。
- 第一条为 1–3 秒调色、颗粒 0.6、暗角 0.65，三项编辑一次事务/revision；正常播放/暂停/片尾、八个跳转时刻、逆向精确复现、微秒时刻桶一致性、半开区间与重开恢复均通过。第二条只新增 0–0.5 秒红色文字，IPC 上下文带 revision 1 及三项已应用参数/范围/顺序；撤销第二条并重开后保留原三项。
- 在第二条文字已撤销后，从正式导出按钮实际输出 **640×360、4.000 秒、H.264/AAC MP4**，一次导出约 **6.177 秒**。这不是实时或长片性能承诺。1、1.5、2 秒的预览/成片颗粒相关性 **0.9646–0.9693**，强度保留比 **0.9295–0.9313**，均值偏移绝对值最多 **1.523**；0.5、3、3.5 秒的区间外平滑区域采样误差 **0/255**。按既定组合统计门限检查，未放宽阈值。
- 脚本 **PASS、退出码 0、无页面错误**，恰好两次固定翻译和一次真实导出。主代理已查看编辑器截图及成片抽帧；左上角色块是人工源视频的逐帧标记，预览中央播放按钮不进入成片。测试窗口已关闭，未关闭或清理用户应用。
- 独立只读证据核查通过：复核脚本、IPC 记录、撤销后导出的顺序及统计值，并重新读取成片元数据；未重复运行测试、Electron 或 AI。核查结论限于上述本地固定翻译与人工素材，不替代真实 AI 或用户人工验收。

本地整链证据及成片：`/tmp/srt-cycle6-local.bAetFB/` 中的 `evidence.json`、`ipc-evidence.json`、`editor.png`、`preview.png`、`export-at-1.5s.png`、`export.mp4`。临时媒体和隔离项目不入库。

### 尚未完成与收尾

- **第六周期没有执行真实 Claude 调用。** 本轮已非阻塞请求两次人工指令/人工项目摘要的授权，但尚未收到回复；不复用第五周期已用完的两次授权，不将固定配方当作真实 AI 推导证据。真实验收脚本已准备但未运行。
- 用户自己的素材观感、原生保存对话框人工点击、打包发行版验收、高分辨率长片性能仍不在此次已验证成果中。
- **内部完成度：第六周期功能与本地自动化/正式入口整链完成。用户可见成果：时间区间内的颗粒、暗角及其与调色的组合已进入可播放 MP4，项目重开、后续新增和撤销有效。** 仅保存本地代码和研发记录，保留分支/工作树；不推送、不合并、不自动开始第七周期。

## 2026-09-08：第六周期真实 AI 联调完成

用户明确要求先完成真实 AI 联调、再上传 GitHub、然后进行第七周期。按此顺序，运行已准备的 `/tmp/srt-cycle6-acceptance.MwZVAY/run.cjs` 一次，退出码 0、PASS、无页面错误。使用正式 Electron 默认本地 Claude CLI 与默认 FFmpeg 导出服务，只隔离测试配置并注入另存为目标；无固定翻译、无真实用户素材、无视频上传。

- 恰好两次实际翻译，耗时 12.497 秒和 20.073 秒。第一条 AI 生成 1–3 秒调色（色温 0.35、饱和度 0.78、对比度 1.08）、颗粒 0.45、暗角 0.55，三项一次事务，顺序完整保留。
- 第二条注入 revision 1 及全部已应用参数/范围/顺序，只追加 0–0.5 秒下方红色“开始”文字；撤销第二条并重开后原三项效果不变。自然播放、暂停、片尾、前后拖动和重开一致。
- 撤销文字后，正式导出按钮生成一次实际 MP4：640×360、24fps、4.000 秒、H.264/yuv420p + AAC，约 6.266 秒。此成片仅含原三项画面效果，不包含已撤销的文字。
- 1、1.5、2 秒的预览/成片颗粒相关性 0.948515–0.950298、RMS 保留比 0.920621–0.922846，均值偏移绝对值最大 1.203706；0.5、3、3.5 秒区间外区域采样误差为零，冻结统计门限未变。
- 主代理已查看 `editor.png` 及 `export-at-1.5s.png`；独立只读核查脚本、IPC、元数据及统计证据通过。证据目录 `/tmp/srt-cycle6-acceptance.MwZVAY/`，测试窗口已自行关闭；没有关闭用户应用。
- 上传前另复跑真实 FFmpeg Node/媒体 **335/335**、零失败/零跳过、退出码 0（4.884 秒）。没有产品或测试代码改动；此前最终完整 Electron **63/63** 为前次证据，本轮不重复冒称。

内部完成度：第六周期实现、自动化验证和本轮获准的真实 AI 整链验证完成。用户可见成果：自然语言推导的调色/颗粒/暗角确实进入成片，下一条新增指令和撤销重开正确。真实素材观感、原生另存为人工交互、打包发行版和高分辨率长片性能仍未由此证明。

接下来先提交本条记录并推送现有 `feature/subtitle-export-cycle0` 到 `trydays/srt`，远端核对一致后才开始已获准的第七周期；不合并主分支。

## 2026-09-08：第七周期个人剪辑技能

按用户指定顺序，第六周期真实联调通过后，已提交并推送 `a9a2bd7f365a95378de54f96c4b254b0e29c0e1d` 到 GitHub `trydays/srt` 的 `feature/subtitle-export-cycle0`，独立读取远端确认完全一致，再开始第七周期。约 11:28 UTC 开工，12:08 UTC 首个可见保存/查看场景通过，12:30 UTC 左右独立本地整链及阅片完成，早于两小时可见检查和四小时止损线。

### 内部完成度与用户可见成果

- 内部：`cd3662c` 接通独立本地技能存储、成功事务参考提取、短技能上下文、生成式提示词及翻译入口第四参数；`2fbf9cd` 接通实际保存/查看/选择/删除 UI 和原有请求生命周期。仍使用同一个执行、预览、持久化、撤销和导出链，不新增渲染能力，默认 AI 目录仍为八项。
- 用户：展开一条仍已应用的成功编辑，可“保存为技能”，填写名称、剪辑意图和偏好。输入区“我的技能”可查看、使用、删除；选择后还需用户发送，可追加当前需求。反问保留选中上下文，退出或任务结束后清除选择。跨项目重开仍能找到技能，删除技能不会删掉视频中已应用的效果。
- 数据：仅保存意图、偏好和参考配方及必要版本/身份/时间字段，不自动复制项目身份、素材路径或生成的字幕全文。复用时把当前项目事实与技能参考一起交给 AI，要求重新推导；不直接执行旧时间区间、位置或文字。未知历史能力仍可查看/删除，输出仍受当前白名单和项目区间校验。
- 不包含自动存技能、自动统计、模型训练、共享、市场、云同步、技能编辑/导入导出、任意代码、新算法或新增视觉积木。能力边界扩展为“用户显式积累和复用剪辑意图”，不是模型或渲染器无限扩展。

### 验证与有限修复

- 按缺失模块/入口的 RED→GREEN 开发。父代理查出“撤销旧记录误清除当前技能”和“原生弹窗关闭抢回输入焦点”两项具体衔接问题，均先用桌面断言复现，再将回调限制到当前请求并修复使用后的焦点恢复。没有因此重构聊天或存储系统。
- 两个任务范围独立审查及全周期 `a9a2bd7..2fbf9cd` 最终独立审查均通过，Critical、Important、Minor 待修项均为 0。最终审查仅读取，不重复运行测试或模型调用。
- 主代理最终独立复跑：实际 FFmpeg Node/媒体 **349/349**（4.929 秒）、完整 Electron **67/67**（3.6 分钟），均退出码 0、零失败、零跳过。完整桌面证据 `/tmp/srt-cycle7-final.fcvIBq/e2e/`。工作代理首次 Node 运行的七项失败仅为沙箱拒绝本机监听，正常获准复跑后全通过；不把初次环境失败隐去或当成产品失败。
- 独立正式入口整链使用隔离 Electron 配置、人工视频、生产解析/IPC/项目/预览和默认 FFmpeg 服务；仅翻译替换为两条标记清楚的固定响应，并指定临时另存为路径。初始 8 秒参考是本地准备的成功组合，不是额外的 AI 调用。
- 实际 UI 保存黑底白字卡片参考（旧区间 6–8 秒），分别在 **640×360/4 秒** 与 **360×640/3 秒** 视频选择该技能，捕获到相同意图/偏好/参考与不同当前视频事实。固定返回的新区间分别为 **3–4 秒**、**2–3 秒**，位置/字号也不同，旧配方未重放；各一次请求、一次事务，重开恢复。
- 两份实际成片均为 **H.264/AAC、48kHz 音轨**，时长分别 **4.000 秒/3.000 秒**；独立读取成片元数据并查看预览、横竖成片抽帧，黑底白字在各自位置可见。撤销第二个目标的最新请求并重开为空；返回第一个目标删除技能，已经应用的编辑仍保留。脚本 PASS、退出码 0、无页面错误，恰好两条固定翻译、两次实际导出；本轮不作渲染性能承诺。
- 验收脚本预检修正了执行回执与重开快照比较、合法默认值处理和原始存储数据断言，均仅为临时脚本修正；未改变产品接口、容差或验收范围。

本地整链与两份成片：`/tmp/srt-cycle7-local.EZ8Azo/`，含 `evidence.json`、`ipc-evidence.json`、`target-a.mp4`、`target-b.mp4`、对应编辑器截图及抽帧。临时媒体和隔离项目不入库，测试窗口已关闭。

### 尚未完成与收尾边界

**第七周期真实 AI 调用为 0 次。** 新的两次人工指令/摘要联调授权尚未收到，未复用第六周期已消耗的授权；本地固定配方只能证明调用衔接和执行正确，不能替代真实模型跨画幅理解/复用效果的验收。用户个人素材、原生保存对话框人工操作、打包发行版与高分辨率长片性能仍另行验收。

内部完成度：第七周期实现、本地自动化/正式入口整链及最终审查完成，真实 AI 验收门保留。用户可见成果：可保存个人剪辑需求、选择技能并追加本次要求，跨项目操作和实际视频输出链已接通。第六周期 GitHub 备份已完成，第七周期仅提交本地代码及记录，保留分支和工作树；不自动再次推送、不合并、不开始下一周期。

## 2026-09-08：第七周期获准后的真实 AI 联调（部分通过，整轮未通过）

用户随后明确允许两次人工文字和项目摘要的真实 Claude 联调，不上传视频。此记录更新上节“真实 AI 调用 0 次／待授权”的历史状态：授权已使用完毕，实际调用 **2 次**，没有重试或第三次调用。

### 实际结果与证据

- 验收对象仍为产品提交 `2fbf9cd`；测试前 HEAD `eb57911` 仅新增既有验收文档。使用隔离 Electron 配置与人工蓝色视频/合成音轨，真实 UI 选择 Claude Code，生产翻译和导出服务均未替换；只包装 IPC 记录、设置隔离资料目录和临时另存为目标。初始 8 秒参考组合由本地正式编辑入口准备，明确标记为固定种子，不算 AI 调用。
- 两条真实请求注入相同技能意图、偏好和旧 6–8 秒参考，当前视频事实分别为横版 **640×360/4 秒**、竖版 **360×640/3 秒**，均无已有操作；捕获的翻译请求不含视频路径或项目身份。没有将视频上传给模型。
- 横版真实翻译 **38.940 秒**成功。模型生成新 **3–4 秒**区间的黑底白字“横版重点”组合，位于画面中下部，并保留透明度淡入/停留/淡出与轻微缩放；不是重放旧 6–8 秒参数。正式应用、单编辑/单事务、请求技能上下文及重开恢复断言通过；实际导出 **0.934 秒**。主代理及独立审查读取成片，确认为 **640×360、4.000 秒、H.264/AAC、48kHz 音轨**；导出抽帧可见卡片。单帧只证明该时刻视觉内容，不代表逐帧动画质量已人工验收。
- 竖版真实翻译 **60.092 秒**失败，返回 `LOCAL_CLI_TRANSLATION_TIMEOUT`，没有可执行指令；失败截图显示“转换编辑指令失败／执行编辑未执行”。应用未进入编辑执行，没有竖版成片。脚本随后因等待成功状态达到 **120 秒**而退出 1；这个界面断言等待与产品 CLI 的 **60 秒**超时是两个不同计时。
- 恰好 **2 次翻译、1 次实际导出**。第二条之后的适配/导出、最新请求撤销、删除技能保留既有编辑步骤未执行；失败后的持久化项目状态未作独立快照断言。此前固定翻译已通过这些本地步骤，但不能替代本次真实模型验收。
- 主代理与独立只读审查结论一致：**横版真实链路通过，竖版翻译超时，完整验收未通过**。证据保存在 `/tmp/srt-cycle7-acceptance.g6gjZj/`，包括 `ipc-evidence.json`、`evidence.json`、`target-a.mp4`、`target-a-frame.png`、`failure.png` 和 `failure-ui.txt`。只关闭测试自行启动的窗口，保留全部证据。

### 原因边界与后续限制

读取现有 `src/local-cli.js` 确认生产翻译等待上限为 60 秒，进程被终止或 `ETIMEDOUT` 会映射为该错误；翻译成功后才进入解析和执行。首条实际成功证明本次至少有一次完整连接及执行，不支持“通用执行链完全没有接上”的结论。但当前错误记录没有保留足够的底层退出/标准错误信息，无法确定第二条是模型生成慢、网络等待还是 CLI 卡住，也不能据此认定竖版必然失败。

本次未修改产品或测试代码，未重复运行此前已通过的 Node349/Electron67 全套回归；未擅自提高超时、改架构、增加监控系统或重试。进一步真实调用需新的用户授权，不自动推送 GitHub。

内部完成度：第七周期实现、本地验收和代码审查已完成；真实 AI 验收尚未通过。用户可见成果：一次真实个人技能复用已生成可播放横版成片；另一条请求目前会显示翻译超时，尚未达到两种画幅均成功的验收条件。

## 2026-09-08：Remotion R1 — 现有卡片迁移

### 范围与实现

用户批准迁移计划后，本次只执行 R1。基线 `38c6263`，实际工作树仍为 `SRTP-worktrees/subtitle-export-cycle0`。不修改 AI 提示词、个人技能协议、严格 v1 存档或 UI 配色，不开始 R2–R4，不调用真实 AI，不提交或推送。

- `createRenderInput` 从完整 `{document,graph}` 和主进程媒体事实生成帧输入；重复编译并比对整张图，不写入临时 URL/FPS 到存档。
- Player 和 renderer 直接引用同一个 `SrtComposition`。源视频只播放一次，现有组合的矩形/文字、半开区间、透明度、缩放、关键帧、叠加和裁剪语义保留。
- 一个小播放控制器承接原播放按钮、拖动、速度、音量和字幕定位。全部节点为 source/group 才整项目采用 Remotion；其它节点仍整项目走旧路径，不静默漏掉效果。
- 主进程创建只读本地资产会话；真实文件校验在媒体探测之前，预览与导出各自持有会话。导出按钮沿用原生保存位置选择、冻结快照、进度、取消及源文件/已有目标保护。
- 实际 `selectComposition` / `renderMedia` 输出 H.264/yuv420p；有源声音保留，无源声音不凭空生成音轨。依赖固定 React 19.2.8、Remotion 系列 4.0.522、esbuild 0.28.2；启动和构建前生成同一场景 bundle。

### 发现并修复的具体问题

- 真正渲染首次失败来自错误指定 Remotion 二进制目录；保留其自身 compositor/FFmpeg，只以已有 ffprobe 检查媒体。
- 容器中的 AAC 尾部填充曾使 4 秒片段报告 4.053333 秒，旧校验误报导出失败。改用视频流时长并在最终拼接明确终点；视频、音频分别检查，未放宽一帧门限。
- 无声源的 OffthreadVideo 会让渲染器生成空音轨；依据实际媒体事实设置渲染静音分支，保留有声源的解码音频验证。
- 重叠预览请求的代次必须在等待清理前冻结；重传必须停掉旧 Player、释放旧素材；等待预览期间新出现的字幕草稿/在途指令必须再次阻止导出。
- Player 错误状态需锁存，同一驱动不能因更新 revision 就重新声称 ready；旧实例错误回调随重传、切引擎和离开页面失效。

上述问题都有先失败、后通过的针对性检查；没有借此增加任务调度平台、重试系统、缓存层或色彩管理系统。

### 验证性质及颜色测量口径

全开关真实 FFmpeg/Remotion/Player 的 Node 测试 **389/389**，零失败、零跳过；原有桌面 67 项默认运行 56 通过/11 跳过，再开真实媒体运行相关 12 项全部通过，覆盖每个跳过项。新增 Remotion 桌面最终复跑 **9/9**，41.4 秒、退出码 0，覆盖横竖屏真实成片、撤销后成片、实际取消、播放/反向跳转、保存重开、重传、草稿保护、错误锁存和整项目兼容选择。此前 8/9 为中间结果，未冒充通过。

测试使用隔离 Electron 配置、合成短片、固定且明确标识的 CLI 响应；真实生产项目/IPC/Player/Remotion 导出执行。保存位置由测试指定，不能称为人工点击原生保存框或真实 AI 联调。

五时点截图曾与裸 RGB 抽帧相差 20/255。只读诊断确认：PNG 内嵌 ICC 下原像素 `(63,113,205)` 转为 sRGB 后精确为 `(42,114,212)`；不能将不同颜色解释路径直接混比。同一 Chromium 的 video→canvas 读取原视频 `(23,101,205)` 与实际成片 `(21,100,203)`，最大差 2/255。未因此调整产品颜色或放宽 12/255 内容检查门限。

R1 的数值验收统一在 sRGB canvas 中读取源帧并栅格化实际 Player SVG DOM，保留其顺序/视口/裁剪/透明度/变换；不从 graph 再造一套图层，不调用产品采样函数作为期望值。真实屏幕截图仍保留用于观感及反向跳转确定性；此方法不宣称最终屏幕显示像素与视频编码 RGB 完全相同，也不覆盖 R3 的源视频效果或专业色彩管理。Remotion 的 [OffthreadVideo 官方说明](https://www.remotion.dev/docs/offthreadvideo) 明确区分预览的原生视频与导出的外部取帧入口。

主代理在最终结果上另行探测四份 MP4：横版 640×360、竖版 360×640；卡片版与撤销版均为 H.264、24fps、96 帧、视频 4.000 秒；AAC 音轨/容器 4.010667 秒，差小于一帧。主代理已查看实际 Player 中文卡片截图及成片 2.5 秒抽帧；中文“重点”完整可辨，红色矩形、裁剪测试文字与位置正确。右下角绿色 M 是故意超长裁剪的测试文字，不是产品 UI 或参考卡片设计。

**字体外观限制：** 两端中文字形可见不同。场景共享 `Heiti SC` 声明并等待 `document.fonts.load/ready`，但未绑定同一字体文件、未证明两种浏览器实际命中同一字体；等待完成不等于目标字体存在。不能声称字体外观一致。本轮按计划单列字体排版差异；后续先核对实际命中字体，再决定是否固定已有合法字体，不顺手建设字体管理器。

最终复跑命令：`npm run test:e2e:remotion -- --workers=1 --output=/tmp/srt-remotion-r1.xDXjmb/accepted-remotion-e2e`。最终 Node 命令启用 `SRT_REAL_REMOTION_PLAYER=1 SRT_REAL_REMOTION=1 SRT_REAL_EXPORT=1` 及已安装的 FFmpeg/ffprobe 路径。`git diff --check` 通过；场景、导出、UI/IPC 及交叉接口独立审查无待处理 Critical/Important。

最终证据：

- `/tmp/srt-remotion-r1.xDXjmb/accepted-remotion-e2e/remotion-export-flow-Remot-8dcbe-then-source-only-after-undo/`：横版卡片、撤销后无卡片成片。
- `/tmp/srt-remotion-r1.xDXjmb/accepted-remotion-e2e/remotion-export-flow-Remot-c1992-then-source-only-after-undo/`：竖版卡片、撤销后无卡片成片。
- `/tmp/srt-remotion-r1.xDXjmb/accepted-remotion-e2e/remotion-player-flow-Remot-95970-ard-frame-deterministically/`：五时点 Player 截图、反向跳转截图与中文预览。
- `/tmp/srt-remotion-r1.xDXjmb/accepted-landscape-frame.png`：主代理查看的实际成片抽帧。

### 阶段边界

内部实现为 R1 三项任务 **3/3**，本地自动化完成；用户可见成果为实际桌面卡片、横竖版成片和撤销后无卡片的成片。到此停止 R1 开发，保留用户人工观感确认。R2 的字幕/独立图层、R3 的源效果与默认切换、R4 的参考视频扩展均未开始。第七周期真实 AI 竖版超时的验收门仍开放。本 Mac 的合成素材与自动化不代表用户素材、原生保存框人工操作、Windows 或安装发行版已验收。

14:36:40 UTC 开始，约 14:52 已出现可检验桌面预览，提前满足 17:06:40 的半程检查点；止损线 19:36:40 UTC 未改变。墙钟时间包含下载、构建及并行测试等待；未逐项精确计时，因此不伪称有效开发/等待或支撑投入百分比已被准确测量。临时证据目录 `/tmp/srt-remotion-r1.xDXjmb/`，不纳入 Git。

15:41 UTC 完成最终自动化结果与交付记录，墙钟约 65 分钟，提前停止，不用满原估时。执行中按计划拆分共享场景、导出和桌面验收任务，依据独立审查修复实际接口问题；完成前验证技能要求最终真实测试与明确未验收项，故未将固定翻译、程序化保存或合成素材当作真实 AI/人工验收。

## 2026-09-09：Remotion R2 — 独立图层与已应用字幕

本阶段跨北京时间午夜，UTC 仍为 9 月 8 日。用户审查范围后以“可以”批准 R2：只迁移独立矩形、独立文字和已应用字幕，与 R1 动画组共享场景。保留实际工作树内全部未提交 R1 变更；先保存触及文件的 R1 快照，按该快照审查 R2，而不是把 Git HEAD 后全部改动算成本轮。

### 内部完成度与用户可见成果

- 内部三项任务完成：共享场景绘制、既有入口接通、桌面实际验收。产品仅改 `src/remotion-input.js`、`app/remotion/layers.jsx`、`app/remotion/SrtComposition.jsx`、`app/editor-subtitles.js` 四个文件；不改依赖、存储、事务、AI、IPC、导出服务或配色。
- 输入准入增加 shape/text/subtitle，继续完整编译和验证 graph/document，媒体信息仍来自既有主进程。含启用源画面效果的图仍整体使用旧引擎；没有静默跳过节点。
- 独立图层复用已有组内文字/矩形几何；字幕读图中已经应用的 segments，遵守节点和片段的半开时段、现有颜色/字号/换行规则。扩展既有字体等待范围，不安装字体或建设字体管理器。预览、导出仍用同一 `SrtComposition` 和帧时间。
- 用户能在同一视频看到独立文字、矩形底板、动画组合及字幕。编辑字幕后，保存只保留草稿并继续阻止导出；应用才同步修改预览和成片。草稿重开、应用重开、最近请求整次撤销有效。字幕改字沿用 `replaceEdit`，不会虚构一层独立的“只撤销改字”事务。
- Remotion 接管时跳过旧字幕 DOM；实际点击字幕卡片也不会重复显示。暂停切回旧引擎、再撤销回 Remotion 的显示正确；已有生命周期足够，没有增加新的播放/重载状态层。

### 验证、发现与处理

- 新准入测试先 9 通过/2 失败，再 11/11；场景先出现三项缺失绘制失败，再通过。真实桌面点击字幕先复现旧层仍可见，再以一个 guard 修复并复跑通过。按测试先行技能执行，但不把无关环境或测试选择器失败冒充功能 RED。
- 第一次横竖屏测试两项失败来自错误索取 `timeline-status`：既有混合字幕记录实际只使用 `subtitle-status`。只纠正测试选择器，未改产品状态界面。第二次 2/2 通过，17.0 秒；证据保留在 `mixed-first/` 和 `mixed-second/`。
- 阅片发现初始竖屏测试配方使用过大的高度相对字号，组中文字按既有规则超画幅裁切。仅将该验收配方字号调整为可读数值并增加实际文字边界断言；未改用户项目参数、裁剪语义、自动排版或颜色。最终再次跑完整 Remotion 桌面验证。
- 主代理最终全开关 Node/媒体 **395/395**，6.143 秒；包含实际 FFmpeg、Remotion 和 Player，零失败/零跳过。命令启用 `SRT_REAL_REMOTION_PLAYER=1 SRT_REAL_REMOTION=1 SRT_REAL_EXPORT=1` 与已安装 FFmpeg/ffprobe 路径。
- 最终 `npm run test:e2e:remotion -- --workers=1 --output=/tmp/srt-remotion-r2.OeB8le/remotion-final`：**12/12**，1.0 分钟；包含全部 R1 用例、字幕引擎切换用例及横竖屏混合流程。原有 `npm run test:e2e` 另启真实媒体开关完整运行：**67/67**，3.6 分钟，产物在 `legacy-final/`。均退出码 0、零失败/零跳过。
- 桌面混合测试从实际聊天输入进入生产解析/项目/预览/导出，翻译及识别结果明确为固定响应，保存位置由测试指定。分别保存草稿、重开、应用、实际导出、再重开及撤销；不以直接注入新字幕图替代应用流程。未调用真实 AI，也未重新验证语音识别质量或人工保存框。
- 主代理独立探测横竖屏修改前后共四份 MP4：640×360 / 360×640，H.264（实际报告 yuvj420p）、24fps、96 帧、视频 4.000 秒；AAC 48kHz，音轨/容器 4.010667 秒，差小于一帧。已查看最终横竖屏预览和前后成片帧，“大家好”变为“已修改字幕”，其余图层仍在。固定矩形区域继续按原 12/255 编码容差检查，字幕区域修改前后 RGB 确实不同；没有声称字幕字形逐像素相同。
- 场景和全轮各一次独立只读审查：无待修 Critical/Important。非阻断建议是单独隔离“字幕节点范围小于片段范围”的单元断言；实现已有双范围检查，不为此展开新边界矩阵。全部五类节点同场的覆盖由实际桌面流程提供。最终 `git diff --check` 通过。

### 投入、边界与证据

15:46:22 UTC 开工；15:59 已有可检验混合桌面预览及横竖版真实成片，提前满足 17:16:22 半程检查点；约 16:08 完成最后记录，墙钟约 22 分钟，未用满三小时上限。准备/权限/基线约 3–4 分钟；15:49–15:59 主要为并行功能接入及针对性验收，15:59–16:05 为最终回归和审查，之后核对成片与记录。工作与工具等待交叠，未逐秒计量，不把代理估算相加冒充精确有效开发或等待时长。初期支撑占比暂高于 25% 已向用户报告；最终按墙钟分段粗估约 14%–18%，不是精确成本核算。没有新增支撑模块或继续扩展基础设施。

最终视频及图片：`/tmp/srt-remotion-r2.OeB8le/remotion-final/remotion-subtitles-flow-Re-aacfa-view-reload-export-and-undo/`（横版）、`remotion-subtitles-flow-Re-2343c-view-reload-export-and-undo/`（竖版）。每个目录含 original/edited MP4、实际 Player 截图及对应导出帧。临时原片、测试资料和证据不入库；R2 时间/范围记录在同一临时根目录的 `progress.md`。

**内部完成：R2 实现及本地自动化验收。用户可见：四类视觉能力同场的真实横竖版成片，以及保存/应用/撤销闭环。** R1 字体实际命中和屏幕颜色解释路径差异仍是限制，不能以同一场景推断完全同一外观。用户素材人工观感、Windows/安装发行版另待验收；第七周期真实 AI 竖版超时仍未解决。R3 源画面效果迁移及默认切换、R4 参考卡片扩展均未开工。本轮不提交、不推送，完成即停止。
